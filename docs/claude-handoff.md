> 기준일: 2026-07-15
> 앱 버전: `0.44`
> 상태: 재산세 계산기에 계산 과정 설명 + 공시가격 입력 시점 안내 추가(0.44) — 0.43 배포 완료 보고 후 사용자가 "계산과정을 계산기 결과 밑에 보여주면 좋을 거 같아요. 당신 생각은?" 질문 → 필자가 "지난 고지서 대조 때도 결국 손으로 단계를 풀어야 했다"는 이유로 찬성·간단한 트레이드오프(문장 위주로 쉽게, 수식 나열은 지양) 제시 → 사용자 "진행하고 첫 공시가격을 입력할 때 언제 가격을 입력해야 하는지 안내문구 넣는것도 포함해서 진행하죠" 승인. `propertyTaxStepsHtml()`이 `calculateHousing()`의 반환값만 읽어(재계산 없음) 계산 과정을 번호 붙은 문장으로 서술(공시가격×공정시장가액비율 → 과세표준상한 적용 여부 → 세율 적용 → 도시지역분 → 지방교육세 → 지분율 → 합계) — 요약 패널의 숫자와 절대 어긋날 수 없는 구조. 공시가격 입력칸에는 부동산 가격공시에 관한 법률 §18·시행령 §43①(공동주택가격은 그 해 1월 1일 기준으로 매년 4월 30일까지 확정공시)을 근거로 "같은 해 1월 1일 기준 값을 넣으라"는 안내문구 추가. 헤드리스 검증(8/8)에서 실제 고지서 시나리오를 계산 과정에 넣었을 때 요약 패널·계산 과정 문장의 숫자가 정확히 일치함을 확인. 재산세 계산기 공유 지분율 + **실제 고지서로 검증 완료**(0.43, 검증 과정에서 자체 정정 1건 포함) — 사용자가 실제 서울시 이택스 재산세(주택1기분) 고지서 사진을 올리며 "1주택자 재산세이고 동일지분 공동명의 2인 주택"이라며 대조를 요청. 처음엔 고지서의 "재산세과표 610,605,000원"을 (과세표준상한이 없다고 가정해) 시가표준액 약 13억5,690만원으로 역산했는데, 사용자가 곧이어 부동산공시가격 알리미 화면(실제 공시가격 2026년 15억7,800만원, 2025년 12억7,800만원)을 제시해 이 가정이 틀렸음이 드러남 — **정정**: raw 과세표준(15억7,800만원×45%=7억1,010만원)이 고지서 과표보다 커서, 실제로는 **과세표준상한액(직전연도+5%)이 발동**한 것이었다. 상한 공식을 거꾸로 풀어 나온 직전연도 과세표준상당액(5억7,510만원)이 2025년 공시가격×같은 45% 비율과 정확히 일치해 정합성을 재확인. 실제 공시가격 + 직전연도 과세표준으로 다시 계산해도 과세표준·세액은 처음과 동일(610,605,000원, 10원 이내 일치) — 최종 세액은 처음부터 맞았지만 "왜 그 값이 나왔는지"의 설명(과세표준상한 발동 여부)이 틀렸었다. 로직 테스트를 실제 검증된 입력값으로 갱신. 대조 과정에서 진짜 UX 갭도 발견: 공유재산은 지방세법 §107①1호에 따라 **과세표준·세율은 물건 전체 기준으로 계산한 뒤 산출세액만 지분율로 안분**하는데, 계산기가 "물건 전체" 결과만 보여줘 사용자가 수동으로 지분율을 곱해야 했음 → 사용자 "진행하죠" 승인 후 `ownershipShare` 파라미터(기본 100%, 공유 지분율 입력 시 최종 세액만 비례 축소, 과세표준·세율 판정에는 영향 없음)를 계산기와 화면에 추가. 헤드리스 검증 중 실제 코드 버그(출력용 `<strong>`과 입력용 `<input>`이 `id="ptShare"`로 중복돼 있던 것)도 발견·수정. 재산세(주택분) 계산기 신설(0.42, 지방세법·시행령 원문 조회로 표준·특례세율·공정시장가액비율·과세표준상한액·도시지역분·지방교육세 구현, 주택만 스코프). 세금 계산 정확성 체크리스트 `TAX_CALC_GUARDIANS` 9개(0.41). 0.33~0.39 전체감사(0.40, 실버그 2건 수정). 클라우드가 정본(0.39). 가계부 삭제(0.38). 마감·마감해제(0.37). **0.16~0.30 배포됨; 0.31~0.44는 브랜치**. 상세는 아래 "완료된 사항" 표 참조.

## 앱 목적 (미션)

이 앱은 초등학생도 이해할 만큼 쉬운 화면으로 수입·지출을 관리하면서, 그 기록이 복식부기 SSOT와 최신 법정서식·세법 근거로 뒷받침되어, 사용자가 세무사 없이도 스스로 국세청에 세무사 수준의 최신 법적 신고자료를 만들 수 있게 AI 역량을 총동원해 돕는다. 쉬움과 정확성 어느 하나도 포기하지 않는다. SSOT는 `docs/accounting-ledger-design-directive-v2.md`의 `0. 최상위 결론`이다.

# Claude Handoff

이 문서는 이 저장소를 처음 받은 Claude가 현재 상태와 변경 금지선을 빠르게 파악하기 위한 인수인계 기준이다. Git의 최신 커밋과 작업 트리가 실제 변경 상태의 최종 기준이며, 이 문서는 중요한 결정의 요약이다.

## 시작 순서

1. `git status --short`, `git log --oneline -5`로 현재 상태를 확인한다.
2. `AGENTS.md`, 루트 `CLAUDE.md`를 읽는다.
3. 요청과 관련된 스킬 문서를 읽는다.
4. 코드·스키마·문서의 현재 상태를 확인한 뒤에만 구현 계획을 제시하거나 변경한다.

## 제품 기준

| 항목 | 확정 내용 |
|---|---|
| 제품 | 대한민국 개인사업자용 간편장부·복식부기 통합 회계 앱 |
| V1 형태 | 단일 HTML + GitHub Pages, PWA/APK 확장 가능 구조 |
| 사용자 | 개인사업자 1인 중심, 향후 복수 사업장·법인 확장 가능 DB |
| 회계 SSOT | 원천거래와 복식 전표. 간편장부는 입력/출력 view |
| 로그인 | Supabase Auth + Google OAuth only |
| 초기 owner | `hanwha27@gmail.com` |
| 로컬 저장 | IndexedDB/localStorage |
| 클라우드 | Supabase Postgres + RLS |
| 증빙 | Cloudinary 이미지/PDF 원본, DB에는 메타데이터 |
| 법정 리포트 | 최신 법정서식 스냅샷 확인 전에는 확정 출력 차단 |
| 앱 버전 | `0.00`에서 시작, 확정 사용자 변경마다 `0.01` 증가 |

## 완료된 사항

| 구분 | 완료 내용 |
|---|---|
| 설계 | V1 상세 설계, 스키마 설계, 화면 흐름, 범위 고정 |
| Supabase | 회계 테이블 38개, RLS 38/38, FK 보조 인덱스 누락 0개 |
| 동기화 | `updated_at` 병합, tombstone, `canonical_version` 최종본 모드 설계 |
| 로그인 | `app_allowed_users`에 owner allowlist seed, Google OAuth/RLS 정책 설계 |
| 운영 | 역할 기반 개발 운영·품질 게이트 및 연구노트 체계 도입 |
| 하네스 | Node 기반 `npm run harness:check` 및 동일 명령을 실행하는 GitHub Actions 도입 |
| 앱 0.01 | 단일 HTML 업무 대시보드, 사업자 설정, 거래 입력, 자동 복식분개, 장부·전표 검토, IndexedDB, JSON 백업·복원, Supabase/Auth adapter, 개발자 Guardian 레지스트리 구현 |
| 앱 0.02 | 운영 Supabase URL과 publishable key 기본 연결, Data API 응답·익명 `businesses` RLS 격리·Google provider 활성 상태 진단, provider 비활성 시 로그인 차단 구현 |
| 앱 0.03 | Google OAuth owner 실로그인과 Google identity·active owner allowlist 확인, 앱 설정·진단 state에 연동되는 단계별 연결 가이드와 복사 기능 구현 |
| 앱 0.04 | 로그인한 owner 권한으로 `businesses`에 격리된 임시 행을 만들어 생성·조회·수정·소프트삭제 격리·정리를 실제 RLS로 왕복 검증하는 설정 자가검증과 개발 기록 상태 표시 구현. 임시 행은 로컬 장부에 저장하지 않고 검증 종료 시 삭제 |
| 앱 0.05 | `관리 → 데이터 관리` 화면 추가. 로컬 저장 상태 확인, JSON 백업·복원, 클라우드 동기화, 거래 부분 삭제(전표·라인 동반 소프트삭제 + tombstone + 감사로그 + 동기화 큐), 이 기기 전체 삭제를 2열 대칭 그리드로 통합. 백업·복원은 설정에서 데이터 관리로 이동 |
| 앱 0.06 | tombstone 기반 다기기 삭제 수렴. `SyncService.convergeTombstones`가 동기화 시 로컬 tombstone을 클라우드에 push(ignore-duplicates)하고 cloud tombstone을 pull해 모든 기기에 소프트삭제를 멱등 적용. RLS SELECT가 `deleted_at is null`로 삭제 행을 숨겨 pull로 전파되지 않던 삭제가 tombstone 채널로 수렴. `tombstones` 테이블·RLS는 기존 스키마 사용(마이그레이션 없음) |
| 앱 0.07 | 소유자 전용 허용 사용자 관리. 설정에 `허용 사용자 관리(owner 전용)` 패널을 추가해 `app_allowed_users`를 조회·추가·차단(status blocked)·재허용(active)하고 변경을 `auth_access_logs`에 기록. bootstrap owner(`hanwha27@gmail.com`)에게만 노출, 소유자 자기 차단 방지 가드. role은 `owner/editor/viewer`, status는 `active/blocked/pending` CHECK 제약에 맞춤. 비허용 계정은 기존 `checkAllowed`가 `status='active'` 아니면 차단·로그아웃. 마이그레이션 없음 |
| 앱 0.08 | canonical version 최종본 지정 배선. 데이터 관리 동기화 카드의 owner 전용 `이 기기를 최종본으로` 버튼이 `SyncService.designateCanonical`을 호출해 전체 로컬 행·tombstone을 클라우드에 업로드하고 `accounting_sync_meta.canonical_version`을 +1. 다른 기기는 기존 소비 경로(`cloudCanonical > localCanonical` → 전체 replace)에서 로컬 전용 변경을 버리고 수렴. `accounting_sync_meta` 쓰기는 bootstrap owner 전용(RLS). 다기기 자동 테스트 9/9 통과. 마이그레이션 없음 |
| 앱 0.09 | Cloudinary 증빙 첨부. `CloudinaryAdapter`(unsigned preset 전용, secret 차단)로 증빙 화면에서 거래별 이미지·PDF 업로드, `AppService.attachEvidence`가 `evidence_files` 메타를 로컬 저장 + 동기화 큐 반영하고 거래를 `attached`로 표시. `evidence_files`를 IDB store(버전 1→2, 추가형)와 `SYNC_TABLE_ORDER`에 편입. 썸네일은 URL 변환으로 파생, 감사로그는 secure_url 대신 public_id만 보관. 설정에 Cloudinary cloud name·preset 입력 폼. `evidence_files` 테이블·RLS는 기존 스키마 사용(마이그레이션 없음) |
| 앱 0.10 | 동기화 무손실 하드닝(다른 앱 교훈 반영). ① 빈 클라우드 가드: canonical replace가 클라우드 businesses 0인데 로컬에 있으면 `EMPTY_CLOUD_GUARD`로 중단해 wipe 방지(데이터 소실 버그 수정). ② `docs/accounting-ledger-data-lifecycle-matrix.md` 신설(도메인×생명주기 배선표 + gap 심각도) + 하네스 `data-lifecycle-matrix` Required 게이트(SYNC_TABLE_ORDER 도메인이 매트릭스에 없으면 실패). ③ 설계지침 §0에 North Star 비타협 원칙(사실 정확성·데이터 무결성·정직한 완료·보안) 명문화. 코드 로직 5/5·게이트 통과 |
| 앱 0.11 | 국세청 업종코드 검색·선택. 국세청 「업종코드-11차 표준산업분류 연계표」(2023 귀속) 1,784개를 파싱해 `NTS_INDUSTRY_CODES` 파생 상수로 index.html에 임베드(대분류 인덱스화 88KB). 설정 업종코드 필드를 블라인드 6자리 입력에서 `IndustryCodes.search`(업종명·코드 키워드) 결과 클릭→코드·업종명 자동 채움으로 교체. 선택값은 **후보**(requires_review)이며 홈택스 공식 조회·경비율 고시 링크로 확인 안내. 원본 xlsx는 미추적, 재생성은 `scripts/build-industry-codes.py`(출처: teht.hometax.go.kr 연계표, 근거: law.go.kr admRulSeq=2100000276582). 수동 코드 입력 fallback 유지 |
| 앱 0.12 | 증빙 제거. `AppService.removeEvidence`가 evidence_files를 soft-delete + tombstone + 감사 + 동기화 큐로 제거(형제 `deleteTransaction`과 대칭)하고, 그 거래에 다른 활성 증빙이 없으면 `evidence_status='not_attached'`로 되돌린다. tombstone은 `SYNC_TABLE_ORDER`의 evidence_files라 `convergeTombstones`가 다른 기기에도 소프트삭제를 적용. Cloudinary 원본 삭제는 서명 API 필요라 미수행(`delete_status='unlinked'`, Edge Function 후보). 증빙 화면에 파일별 제거 버튼. 생명주기 매트릭스의 evidence_files 삭제 gap 해소. 마이그레이션 없음 |
| 앱 0.13 | 다른 앱 프롬프트 교훈 반영 + 인적용역 지원. ① 사업자등록번호를 `(선택)`으로 명확화하고 인적용역(업종코드 94로 시작, 예: 940600 자문·고문료)은 없을 수 있음을 안내(등록번호는 기존에도 선택). 업종 선택 시 94코드면 `industryPickedHtml`이 인적용역 힌트 표시. ② `scripts/tests/logic.test.mjs`를 커밋하고 하네스 `logic-tests` Required 게이트로 등록 — 실제 앱 IIFE를 VM에 로드해 복식부기(buildPosting 균형)·validateJournal·calculateAmounts·Utils·IndustryCodes를 실코드 검증 + 동기화/삭제 알고리즘 특성화(18 assertion). 게이트 양방향 확인(파손→FAIL, 복원→PASS). `IndustryCodes`를 테스트 훅에 노출. ③ CLAUDE.md에 AI 개발 규율(실측 진단·프로덕션 증거·정직한 완료·결함→게이트·단일파일 봉합점·최소변경) 추가 |
| 앱 0.14 | 입력 자동화 ①그룹(휴먼에러 방지, 외부 자료 불필요). ① `AccountingDomain.isValidBusinessNumber` 체크섬(가중치 1,3,7,1,3,7,1,3,5)으로 사업자등록번호 오타 차단(입력 시에만, 인적용역 등 없으면 통과). ② 대표자명 미입력 시 Google 프로필 이름(`user_metadata.name/full_name`) 자동 채움. ③ 업종코드 선택 시 그 코드의 대분류(94→프리랜서, 그 외 `BROAD_INDUSTRY_BY_MAJOR` 매핑)로 큰 업종 자동 설정. 로직 테스트 +3(체크섬 실코드 검증, 총 21). 마이그레이션 없음 |
| 거버넌스(앱 버전 무변경) | 실행 루프(에이전트 루프) 절 신설. 사용자가 외부 "loadout" 자료의 "에이전트 루프"(자율성) 개념을 보여주며 적용 가능성 질문 → 여러 전담 에이전트로 역할을 쪼개는 대신, 0.33~0.39 구간에서 반복 검증된 단일 실행 순서(브랜치 확인→조사→설계→구현→로직테스트→하네스→헤드리스 실증검증→문서4종 갱신→버전→커밋)를 `docs/skills/accounting-development-governance-skill.md`에 명문화하는 쪽을 추천, 사용자 승인. 실제 있었던 사고(배포 직후 브랜치 확인 누락으로 main에 잘못 커밋할 뻔함, push 전 발견해 복구)를 근거 사례로 포함. `CLAUDE.md` "먼저 적용할" 목록에 참조 추가. 스킬 버전 0.06→0.07 |
| 앱 0.44 | 재산세 계산기 — 계산 과정 설명 + 공시가격 입력 시점 안내. 배경: 0.43 완료 보고 후 사용자 "계산과정을 계산기 결과 밑에 보여주면 좋을 거 같아요. 당신 생각은?" → 필자가 "0.43에서 고지서 대조할 때도 결국 손으로 단계를 풀어야 했다"는 근거로 찬성, 트레이드오프(수식 나열보다 문장 위주로 쉽게)를 제시 → 사용자 "진행하고 첫 공시가격을 입력할 때 언제 가격을 입력해야 하는지 안내문구 넣는것도 포함해서 진행하죠" 승인(두 가지 요청을 한 번에 처리). **조사**: 공시가격 입력 시점 안내를 위해 부동산 가격공시에 관한 법률(§18)·시행령(§43①)을 새로 조회 — 공동주택가격은 그 해 1월 1일 기준으로 매년 4월 30일까지 정기공시(신축 등으로 없으면 6월 1일 기준 9월 30일까지 추가공시)됨을 확인, 재산세 과세기준일(6월 1일)엔 이미 그 해 값이 확정돼 있으므로 "같은 해 1월 1일 기준 값"을 넣어야 한다는 안내를 근거를 갖춰 작성. **구현**: `propertyTaxStepsHtml(input, res)` 신설 — `PropertyTax.calculateHousing()`의 반환값(rawTaxBase/taxBase/capApplied/appliedRateTable/wholePropertyTax 등)만 그대로 읽어 번호 붙은 문장으로 서술(재계산 없음 — 요약 패널 숫자와 절대 어긋날 수 없는 구조). 단계: ①공시가격×공정시장가액비율 ②과세표준상한액 적용 여부(조건부) ③세율 적용 ④도시지역분 ⑤지방교육세 ⑥지분율 적용(조건부, 지분율<100%일 때만) ⑦합계. `propertyTaxPanelHtml()`이 `input`도 받도록 시그니처 변경(두 호출부— 최초 렌더·`ptRecalc`— 모두 갱신). 공시가격 입력칸에 위 조사 내용을 근거로 한 field-help 추가(realtyprice.kr 안내 포함). `.calc-steps`/`.calc-step`/`.calc-step-num` CSS 신설(기존 `.est-out`/`.est-row` 시각 언어 재사용). **검증**: 로직 테스트 149 유지(순수 계산 함수 무변경, UI 렌더링 함수라 도메인 테스트 대상 아님 — 기존 관례와 일관). `npm run harness:check` 12/12 Required 통과. 헤드리스 Chromium 8/8: 공시가격 입력 안내문구("1월 1일 기준"·"4월 30일"·"realtyprice.kr" 포함) 노출 확인, 0.43의 실제 고지서 시나리오(15억7,800만원+1세대1주택+직전연도 과세표준 5억7,510만원+지분 50%)를 계산 과정에 넣었을 때 "과세표준상한액"·정확한 캡핑값(610,605,000원)·지분율 50%가 계산 과정 문장에 그대로 등장, 계산 과정 마지막 줄의 합계 숫자가 위 요약 패널 합계와 정확히 일치함을 확인. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.43 | 재산세 계산기 공유 지분율 + 실제 고지서 검증. 배경: 0.42 완료 보고 후 사용자가 실제 서울시 이택스 재산세(주택1기분) 고지서 사진을 올리며 "이게 실제고지서야. 차이 보장해보자. 1주택자 재산세이고 동일지분 공동명의 2인 주택이야" 요청. **대조**: 고지서는 1기분(상반기 절반)이라 2배 환산. 지방세법 §107①1호("공유재산은 그 지분에 해당하는 부분에 대해 그 지분권자가 납세의무")를 §107①2호(건물·토지 소유자가 다른 유사 사례에서 "산출세액을... 안분계산"이라 명시)와 함께 확인해, 공유 재산은 **과세표준·세율을 물건 전체 기준으로 계산한 뒤 산출세액만 지분율로 나눈다**는 원칙을 확정. 처음엔 고지서의 "재산세과표 610,605,000원"을 (상한 미적용을 가정해) 1세대1주택 특례 공정시장가액비율 45%(6억 초과 구간)로 역산해 시가표준액 약 13억5,690만원을 도출했는데, 곧이어 사용자가 부동산공시가격 알리미 화면(실제 공시가격 2026년 15억7,800만원, 2025년 12억7,800만원)을 제시해 이 가정이 틀렸음을 확인 — **정정**: raw 과세표준(15억7,800만원×45%=7억1,010만원)이 고지서 과표보다 커서 실제로는 과세표준상한액(직전연도+5%)이 발동한 것이었고, 상한 공식을 거꾸로 풀면 직전연도 과세표준상당액 5억7,510만원이 필요한데 이는 2025년 공시가격×같은 45%와 정확히 일치. `PropertyTax.calculateHousing({marketValue:1578000000, isOneHouseholdOneHouse:true, priorYearTaxBase:575100000})`로 다시 계산하면 과세표준이 처음과 똑같이 610,605,000원(시가표준액이 9억을 넘어 특례세율이 아니라 표준세율표 적용)이 나와, 최종 세액 자체는 처음부터 맞았지만 설명(왜 그 과표가 나왔는지)이 틀렸었음을 확인. 50% 지분으로 나누면 재산세 본세(예측 906,210원 vs 실측 906,200원)·도시지역분(전자송달 공제 전, 예측 427,424원 vs 실측 427,420원)·지방교육세(예측 181,242원 vs 실측 181,240원)가 전부 **10원 이내로 정확히 일치**함을 확인. 차이 나는 부분(800원 전자송달·자동이체 공제는 세법이 아니라 지자체 행정 할인이라 스코프 밖, 지역자원시설세 31,740원은 이미 안내된 미구현 항목)도 전부 원인이 명확했다. 이 과정에서 계산기가 "물건 전체" 결과만 보여주고 지분 안분은 사용자가 수동으로 해야 했던 실제 UX 갭을 발견 → 제안·사용자 "진행하죠" 승인. **구현**: `PropertyTax.calculateHousing()`에 `ownershipShare` 파라미터 추가(기본 1=100%, 과세표준·세율표 선택에는 영향 없이 최종 세액 3종(재산세·도시지역분·지방교육세)에만 곱함, `wholePropertyTax`/`wholeUrbanAreaLevy`/`wholeLocalEducationTax`/`wholeTotal`로 물건 전체 금액도 함께 반환). 화면에 "공유 지분율(%)" 입력(기본 100) 추가, 지분율<100%일 때만 "물건 전체 재산세 합계"·"공유 지분율" 행과 "내 지분 몫" 라벨을 노출(100%일 때는 기존 단독소유 UX 그대로 — 회귀 없음). **헤드리스 검증 중 실버그 발견·수정**: 출력용 `<strong id="ptShare">`와 입력용 `<input id="ptShare">`가 id가 중복돼 있어(HTML 무효, `querySelector`가 입력 요소를 먼저 잡아 지분율 표시가 항상 빈 문자열로 나옴) 지분율 표시행이 실제로는 안 뜨고 있었음 — 출력 요소를 `ptShareOut`으로 분리해 수정. **검증**: 로직 테스트 +11(총 149) — `ownershipShare` 생략 시 기존 100% 결과와 완전 동일(회귀 없음), `wholeTotal`이 지분율과 무관하게 항상 물건 전체 계산과 일치, 50% 지분이 정확히 절반(반올림), 100% 초과·음수 지분율의 clamp(0~1), 그리고 **실제 공시가격·직전연도 과세표준을 입력값으로 쓴 이 고지서 시나리오 자체를 오라클 테스트로 고정**(과세표준상한액이 실제로 발동함을 `capApplied===true`로 명시 확인 + 재산세과표 정확 일치 + 3개 세목 10원 이내 일치를 코드로 잠금 — 이제 회귀가 생기면 이 테스트가 잡는다). `npm run harness:check` 12/12 Required 통과. 헤드리스 Chromium 8/9(콘솔 에러 1건은 기존과 동일한 파비콘 404, 앱 로직과 무관): 지분율 기본값 100 확인, 고지서 시나리오 입력 시 과세표준·재산세 본세가 예측치와 정확히 일치, 지분율 50%일 때 "50%" 표시행과 "내 지분 몫" 라벨 노출, 100%로 되돌리면 두 요소 모두 사라짐(단독소유 UX 원복) 확인. **정확성 성과**: 0.42에서 "국세청 공식 사례로 대조 못함"으로 남겨뒀던 위험이 실제 사용자 고지서로 해소됨 — `EstimatedIncome`(작성사례 대조) 이후 두 번째로 실사례 검증된 계산기. **남은 위험**: 지역자원시설세(소방분)·토지·건축물·선박·항공기 미구현은 그대로, 1세대1주택 특례세율의 2027년 이후 연장 여부 재검토 필요도 그대로. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.42 | 재산세(주택분) 계산기. 배경: 사용자가 좌측 메뉴 하단(가이드 아래) 빈 자리를 스크린샷으로 표시하며 지자체 재산세 안내 팜플렛 사진과 함께 "재산세 계산기를 여기에 설계하려고 합니다. 법령을 꼼꼼히 확인해서 만들어주세요" 요청. **조사(법령 MCP 원문 대조, 지방세법 MST 282559·시행령 MST 287223, 둘 다 2026-07-01 시행 현행본)**: 제104조(정의)·제105조(과세대상)·제110조(과세표준)·제111조(표준세율)·제111조의2(1세대1주택 특례세율, 9억원 이하 한정)·제112조(도시지역분 0.14%)·제114조(과세기준일 6.1)·제115조(납기)·제151조(지방교육세=재산세액의 20%), 시행령 제109조(공정시장가액비율: 일반 60%, **2026년도 한정** 1세대1주택 특례 43/44/45%·9억원 초과도 포함)·제109조의2(과세표준상한액=직전연도+5%)·제110조의2(1세대1주택 판정, 예외 12개 호로 매우 복잡함을 확인). `get_law_text`가 세율표를 표(HTML table)로 반환해 텍스트 추출이 비어, 웹서치로 다중 출처 교차검증(0.1~0.4%/0.05~0.35% 세율 확인). **팜플렛과의 차이를 직접 발견·정정**: 사용자가 준 팜플렛의 "세부담상한(재산세액 대비 105~130%)" 절은 2024년 세법개정으로 **폐지되고 과세표준상한제(과세표준 대비 5%)로 대체된 옛 제도**임을 법령 원문에서 확인 — 팜플렛을 그대로 베끼지 않고 현행 제도로 바로잡음. **설계**: 스코프를 주택분만으로 의도적으로 좁힘(토지·건축물·선박·항공기, 지역자원시설세(소방분)는 화면에 "다음 업데이트 예정"으로 명시 — 하드룰: 미구현을 완료된 것처럼 보이지 않음). 1세대1주택 판정(시행령 §110의2, 다주택·상속·혼인·임대주택 등 12개 예외)은 앱이 자동 판정하지 않고 사용자가 직접 체크하도록 함(추측 판정의 위험을 사용자에게 떠넘기지 않되, 과도한 자동화도 하지 않음). **구현**: `PropertyTax`(index.html) — `fairMarketRatio`/`bracketsFor`/`progressiveTax`/`calculateHousing()` 순수 함수. 새 나브 그룹 "계산기 > 재산세 계산기"(`renderPropertyTaxCalculator`), 공시가격·1세대1주택 체크·직전연도 과세표준(선택)·도시지역분 체크 입력에 실시간 재계산(기존 추계소득 계산기 `estRecalc` 패턴 재사용). `AUTO_SYNC_UNSAFE_ROUTES`에 `propertyTax` 추가(0.40에서 고친 것과 같은 클래스의 버그를 새 입력 화면에서 재도입하지 않기 위해). **실버그 발견·수정**: 경계 연속성 로직 테스트를 작성하던 중 1세대1주택 특례세율표의 1억5천만~3억원 구간 누진공제 상수를 120,000원으로 잘못 입력했던 걸 실제로 발견(정답은 180,000원) — 테스트가 배포 전에 잡았다. **검증**: 로직 테스트 +24(총 138) — 공정시장가액비율·세율표 선택 경계값(3억/6억/9억), 모든 구간 경계에서 세액 연속성(1원 이내), 손으로 검증한 시나리오 3건(3억원 1세대1주택→합계 299,400원, 1억원 일반주택→156,000원, 10억원+낮은 전년도 과세표준→과세표준상한 적용 1,243,500원), 상한 미적용·도시지역분 미포함·0원 입력 경계. `npm run harness:check` 12/12 Required 통과. 헤드리스 Chromium 8/9(콘솔 에러 1건은 파비콘 404 — 앱에 favicon 링크 자체가 없어 브라우저가 자동 요청하는 것, 앱 로직과 무관): 나브 항목 노출, 3억원 1세대1주택 계산이 손검증값과 정확히 일치, 1세대1주택 체크 해제 시 즉시 60%/표준세율표로 재계산, 도시지역분 체크 해제 시 0원, 과세표준상한액 적용 문구 노출을 화면에서 직접 확인. **남은 위험**: (1) 국세청 공식 계산 사례로 대조하지 못함(추계소득 계산기와 달리 사례를 못 구함 — 내부 일관성만 검증), (2) 지역자원시설세(소방분)·토지·건축물·선박·항공기 미구현, (3) 1세대1주택 특례세율은 2021~2026년 한시 규정이라 2027년 이후 연장 여부에 따라 재검토 필요. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.41 | 세금 계산 정확성 체크리스트(`TAX_CALC_GUARDIANS`). 배경: 사용자가 "세금 계산기 관련 에이전트 있을까?" → 스킬·플러그인 카탈로그 검색 결과 없음, 저장소 안엔 이미 `GOVERNANCE_AGENTS`(개발 프로세스 역할 체크리스트) 패턴이 있음을 확인 → 사용자가 TaxLawSource/TaxRuleModeler/TaxCalculation/TaxBoundaryTest/TaxOracle/TaxChangeDiff/TaxExplanation/PlaywrightTaxQA/SecurityPrivacy 9개 에이전트 표를 제시하며 의견 요청. **검토**: 9개를 성격이 다른 두 그룹으로 나눠 판단 — ① 법령수집·규칙모델링·계산구현·설명생성은 "법을 읽은 사람과 코드로 옮긴 사람이 갈리면 미묘한 예외조항(호·초과율 구간 등)이 새는" 위험이 커서 분리 반대, 지금의 단일 에이전트 실행 루프(조사→설계→구현)를 그대로 유지하는 게 맞다고 판단(0.18~0.20 추계소득 계산기를 이 방식으로 만들어 국세청 작성사례 수치와 원 단위까지 맞춘 전례 근거). ② 경계값검증·공식사례대조·개정비교·보안·화면QA는 반대로 "만든 사람과 다른 시선"이 핵심 가치인 검증 작업이라 독립 에이전트 분리가 정답이라고 판단, 오늘 이미 실행한 0.40의 4-에이전트 병렬 "전체감사"가 정확히 같은 패턴이고 실제 버그 2건을 잡은 전례를 근거로 제시 → 상시 실행이 아니라 **트리거 기반**(경비율·기준금액 국세청 개정 시, 세금 계산기 기능 변경 시)으로 묶어 나중에 워크플로 스크립트를 작성하자고 제안 → 사용자 "니 추천대로 보수적으로 적용하자. 핵심은 정확성이니까" 승인. **구현(이번엔 문서화만, 보수적으로)**: `TAX_CALC_GUARDIANS`(9개, `GOVERNANCE_AGENTS`와 동일 4칸 형식·전부 `manual_only` — 이 목록 자체는 자동 강제가 아니라 체크리스트임을 명시) 신설, `renderDeveloper()`의 Guardian 레지스트리(`registry` 배열)에 합류시켜 개발 기록 화면에 카드로 노출, 테스트 훅에도 노출. **의도적으로 안 한 것**: 경계값·공식사례대조·개정비교·보안·QA 5개를 실제 병렬 감사 워크플로 스크립트로 만드는 건 지금 트리거할 대상 변경이 없어 보류(과잉 준비 방지) — 세금 계산기 관련 기능을 추가·수정하거나 경비율 개정 시즌에 실제로 작성하기로 함. 계산 로직 자체(EstimatedIncome/ExpenseRateMethod/BookkeepingDuty)는 무변경. **검증**: `npm run harness:check` 12/12 Required 통과. 헤드리스 Chromium 6/7(콘솔 에러 1건은 외부 CDN 차단 — 기존 릴리스들과 동일하게 제외 대상): `TAX_CALC_GUARDIANS` 9개·id 유일성·전부 `manual_only` 확인, 개발 기록 화면에 실제로 9개 카드(`tax_oracle_agent`·`playwright_tax_qa_agent` 등)가 렌더링됨을 DOM에서 직접 확인, 전체 Guardian 카드 수 정확히 증가 확인. 로직 테스트 114 유지(정적 체크리스트 데이터라 DOMAIN_GUARDIANS/GOVERNANCE_AGENTS와 동일하게 전용 로직 테스트 없음, 기존 관례와 일관). 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.40 | 0.33~0.39 전체감사 + 버그 2건 수정. 배경: 0.39 배포·거버넌스 문서화 완료 후 사용자 "이쯤에서 전체감사 진행하는건 어때?" → 넓은 범위(0.33~0.39, 다중 가계부·자동동기화·문서·보안 4개 축)를 병렬 조사가 유리한 예외 케이스로 판단, `general-purpose` 서브에이전트 4개를 동시에 launch(각각 격리+계단식삭제 완전성, 자동동기화 연동, 문서/버전/게이트 정합성, 보안·권한 모델 담당). **감사 결과**: 문서/게이트 정합성(4번째)은 문제없음. 보안(RLS·권한)도 문제없음 — 신규 파괴적 메서드(createLedger/deleteLedger/closePeriod/reopenPeriod/exportCloudBackup/resetLocalFromCloud) 전부 최종 방어선이 Postgres RLS(`owner_user_id=auth.uid()` 체인)이고, 클라이언트 JS 체크 부재는 기존 'viewer' 롤 자체가 애초에 어디서도 강제되지 않던(표시 전용) 기존 베이스라인과 일관됨(회귀 아님). **실버그 2건**: ① `AUTO_SYNC_UNSAFE_ROUTES`에 `closing`(마감 화면, 0.37에서 자유 텍스트 `closeReason` 필드 추가됨)이 빠져 있어, 마감 사유 입력 중 백그라운드 자동동기화(0.35)가 돌면 `render()`가 전체 재렌더되어 입력값이 사라질 수 있었음(0.37 출시 당시 헤드리스 검증이 자동동기화 인터럽트 시나리오를 안 다뤄서 놓침). ② `AppService.tombstone(entityType, entityId)`가 `business_id`를 항상 `state.business?.id`(활성 가계부)로 고정 — `deleteLedger`가 비활성 가계부를 지울 때(가계부 관리 화면은 비활성 가계부에도 삭제 버튼을 보여줌) 그 가계부의 tombstone들이 실제로는 활성 가계부 것처럼 잘못 찍힘. 현재는 `tombstone.business_id`를 읽는 코드가 전무해 실질 데이터 손실·타사용자 노출은 없었으나(RLS는 같은 소유자 소유 가계부라 여전히 통과), 향후 이 필드를 조회·감사하는 기능이 생기면 잘못된 값을 신뢰하게 될 잠재적 정합성 결함. **수정**: `AUTO_SYNC_UNSAFE_ROUTES`에 `'closing'` 추가. `tombstone()`에 3번째 인자 `businessId`(생략 시 기존처럼 활성 가계부 폴백) 추가, `deleteLedger`의 유일한 호출부가 삭제 대상 가계부 id를 명시 전달하도록 수정(다른 4개 호출부는 전부 활성 가계부 컨텍스트라 인자 생략 유지, 동작 불변). **검증**: 로직 테스트 +4(총 114, `tombstoneBusinessId` 순수 미러 — 명시값 우선·생략시 폴백·활성값 없음·명시적 null 구분). 개념원장 앵커를 새 시그니처(`tombstone(entityType, entityId, businessId)`)로 갱신. 헤드리스 Chromium 7/7: `AUTO_SYNC_UNSAFE_ROUTES`에 `closing` 포함+기존 3개 유지 확인, closing 화면에서 입력 후 `hashchange` 강제 재렌더(동일 라우트 유지, 자동동기화 완료 핸들러와 동일 호출부 재현)로 값이 사라짐을 먼저 확인(가드가 실제로 막아야 하는 문제가 진짜임을 증명) → 가드 배열에 `closing`이 이제 포함되므로 그 정확한 호출이 스킵됨을 코드 경로로 확인. 가계부A/B 생성(B가 활성)→비활성 A 삭제→IndexedDB `tombstones` 스토어 직접 조회로 A의 tombstone 27건 전부 `business_id===A.id`, B로 잘못 찍힌 건 0건 확인. 앱 JS 에러 0. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.39 | 클라우드가 정본 — 백업·복원·강제 새로고침 완성. 배경: "우리 앱에 전체 데이터 백업 복원 기능 있나요?" → 있으나 로컬 전용이라고 정확히 답변 → 사용자 "...데이터 백업 저장 복원 기능 제대로 알파부터 오메가까지 구현하죠. local 저장소는 보조캐시저장 정도 역할이고 정본은 supabase로" → 조사 후 "백업 기능만 완성(A, 안전)" vs "동기화 엔진 자체를 클라우드 항상 승리로 재설계(B, 구조 변경)" 제시 → 사용자 "A로 하되... 기기별로 달라지지 않게 시스템적으로 막는게 핵심". **조사**: 평상시 동기화는 로컬·클라우드 동등 권위 LWW(updated_at 최신 승리) 병합, 클라우드 우선을 강제하는 유일한 기존 수단은 owner 전용·수동인 `designateCanonical`(0.08)뿐이었고, 백업/복원은 100% 로컬 IndexedDB만 다뤄 클라우드 직접 백업/복원 기능이 아예 없었음. **설계**: 동기화 엔진(LWW)은 안 건드림(검증된 로직 보존) — 대신 "정본을 믿을 수 있는 도구"를 추가. "복원 후 모든 기기에 반영"을 새로 만들려다 기존 `designateCanonical`이 정확히 그 역할(로컬을 클라우드에 강제로 올리고 canonical_version을 올려 다른 기기가 무조건 따라오게 함)을 이미 하고 있음을 발견해 재사용(`restoreBackup()` 뒤에 이어 호출). `exportCloudBackup`/`resetLocalFromCloud`는 `remote.*`를 직접 호출하므로 Remote Adapter 계층인 `SyncService`에 배치(AppService에 넣었다가, AppService가 지금까지 remote를 직접 참조한 적이 없었음을 확인하고 옮김 — 0.35 계층 원칙 유지). **구현 중 발견한 버그**: `restoreBackup`이 백업 파일에 없는 키(예: 클라우드 백업엔 없는 sync_queue·app_research_notes)를 빈 배열로 취급해 그 저장소를 통째로 비우고 있었음 — 클라우드 백업을 복원하면 아직 안 올라간 로컬 변경이 사라지는 실질 데이터 손실 버그. "키가 없으면 안 건드린다"로 수정. **구현**: `SyncService.exportCloudBackup()`(SYNC_TABLE_ORDER+tombstones를 `remote.pullTable`/`pullTombstones`로 직접 조회), `SyncService.resetLocalFromCloud()`(canonical_version 비교 없이 무조건 클라우드로 로컬 전체 교체, EMPTY_CLOUD_GUARD 재사용). 데이터 관리 화면 카드 재구성("로컬 캐시"/"클라우드 동기화"+새로고침 버튼/"백업"(클라우드 우선)/"복원"(이 기기만 · 모든 기기에 반영)), 상단 안내를 "정본은 클라우드" 원칙으로 재작성. 개념원장에 "정본은 클라우드, 로컬은 보조 캐시" 신설(anchor: `resetLocalFromCloud`). **검증**: 로직 테스트 +5(총 110, `restoreScope` 순수 미러 — 부재 키 보존·명시적 빈배열 적용·잘못된 타입 거부). 헤드리스 Chromium: 데이터 관리 문구 "정본" 확인, 비로그인 시 클라우드 버튼 비활성/숨김 확인, 이 기기 백업이 `source:'local'`+sync_queue 키 포함 확인, **클라우드 형태 백업(sync_queue 키 없음)을 "이 기기만 복원"으로 복원해도 동기화 대기 32건→32건 그대로 유지 확인**(수정 전이면 0건으로 wipe될 시나리오). 앱 JS 에러 0. **남은 위험**: 동기화 엔진(LWW) 자체는 미변경(옵션 B는 범위 밖), 클라우드 연동 세 기능은 실제 로그인 필요해 헤드리스 전 구간 검증 불가(수동 확인 항목). 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.38 | 가계부 삭제(계단식 소프트삭제). 배경: 0.37 배포 완료 보고 후 사용자 "가계부 삭제기능 추가하고 배포하자" — 0.36에서 "이번 범위 밖(형제 패턴 있어 후속 용이)"로 남겨둔 항목을 이어 처리. **설계**: 계정과목·거래처 삭제(자기 자신만 소프트삭제, 과거 거래는 그대로)와 달리, 가계부 삭제는 그 가계부에 속한 모든 자료(business_sites/ledger_period_settings/accounts/counterparties/source_transactions/journal_entries/journal_entry_lines/evidence_files/period_closings)를 함께 소프트삭제 — 안 그러면 화면엔 안 보여도 로컬·클라우드에 orphan 데이터가 영영 남음. audit_logs는 append-only라 예외(가계부 존재·삭제 이력은 감사 기록으로 남아야 함). 활성 가계부 삭제 후 처리를 별도로 안 만들고, 0.36에서 정확히 이 경우를 대비해 만든 순수 함수 `pickActiveBusiness`(설정 id가 목록에 없으면 첫 번째로 폴백)에 그대로 위임 — `reload()`만 다시 부르면 자동 정리됨. 마지막 가계부 삭제도 막지 않음(`deleteTransaction`이 마지막 거래 삭제를 안 막는 것과 동일 원칙) — `state.business`가 null이 되면 기존 렌더러가 이미 안전하게 처리(빈 폼). **구현**: `AppService.deleteLedger(businessId)`(9개 자식 테이블 스코프 소프트삭제+tombstone+동기화 큐+감사로그 1건). 가계부 관리 목록에 삭제 버튼(활성/비활성 모두), 확인창에 삭제 범위 구체적 명시 + 마지막 가계부일 때 추가 경고. `errorMessage()`에 `LEDGER_NAME_REQUIRED`/`LEDGER_MODE_INVALID`/`LEDGER_NOT_FOUND` 매핑 추가(0.36에서 놓친 gap도 같이 수정). **검증**: 로직 테스트 105 유지(IndexedDB 의존 카스케이드라 순수 함수 미러 없음, 헤드리스로 전체 경로 검증). 헤드리스 Chromium: 가계부A(거래1)+가계부B(거래1) → 활성인 B 삭제(확인창 문구 확인) → A로 자동 전환, B 거래 안 보이고 A 거래 유지(격리 확인) → 마지막 A도 삭제(마지막 경고 확인) → 빈 새 가계부 입력 폼으로 정상 복귀(크래시 없음). 앱 JS 에러 0. **남은 위험**: 삭제된 가계부 복구 UI 없음(계정과목·거래처와 동일 한계), `deleteLedger`는 로직 테스트 회귀 잠금이 없어 카스케이드 테이블 목록 변경 시 헤드리스 재검증 필요. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.37 | 마감·마감해제(되돌릴 수 있는 월 잠금). 배경: 0.36 완료 보고에서 "가계부 추가 → 마감" 순서 제안·승인 → 배포 후 사용자 "남은 것을 당신 추천대로 진행해주세요" → 마감 이어서 구현. **근본 상태**(사전 조사): `renderClosing()`은 표시 전용, 실제 잠금 버튼 없었음(코드에 "마감 후 수정 통제와 재개방 감사로그가 완성되기 전에는..." 안내문구로 명시). `period_closings` 스키마는 처음부터 `status`·`closed_at`·`reopened_at`을 갖추고 있어 앱 레이어만 채우면 되는 구조. **설계**: 이름을 실제 동작에 맞춤(영구마감이 아니라 되돌릴 수 있는 월 단위 잠금, 화면에 "영구 삭제가 아닙니다" 명시). 재개방은 행 삭제가 아니라 `status`를 `'open'`으로 되돌리는 갱신이라 `closed_at`/`reopened_at` 이력이 남음. 개인 가계부 모드에도 노출(`PERSONAL_HIDDEN_NAV`에서 `closing` 제거) — 마감은 세무 전용이 아니라 "이번 달 정산 끝났으니 잠그기"라는 보편적 필요. **구현**: `AccountingDomain.isDateClosed(closings, dateStr)`(순수) → `validateTransaction(input, closings)`의 기존 `data-error="transactionDate"` 슬롯에 연결(신규 UI 배선 불필요), `deleteTransaction`도 같은 함수로 방어(`PERIOD_CLOSED`). `AppService.closePeriod({year,month,reason})`/`reopenPeriod(closingId,reason)`(감사로그·동기화 큐 배선, 같은 달 중복 마감은 `CLOSING_OVERLAP`로 차단). `renderClosing()` 전면 재작성(마감 기록 목록+연/월 선택 마감 폼). `errorMessage()`에 `PERIOD_CLOSED`/`CLOSING_OVERLAP`/`CLOSING_NOT_FOUND`/`CLOSING_ALREADY_OPEN` 안내문 추가. `DOMAIN_GUARDIANS`의 `period_close_guardian`을 `manual_only`→`implemented`로 갱신. **의도적으로 다루지 않은 범위**: 간편장부 Excel 가져오기 경로는 마감 검증 미적용(가져오기 자체가 개인 모드에서 숨겨져 있고, 이미 945건 실사용 검증된 기존 파이프라인에 손대는 리스크 대비 이득이 작다고 판단), 연 단위 마감 미지원(월 단위만). **검증**: 로직 테스트 +9(총 105, 경계값·재개방·소프트삭제 무시·validateTransaction 통합 확인). 헤드리스 Chromium: 개인 가계부 생성 → 거래 저장 → 이번 달 마감 → 같은 달 새 거래 저장 시도 시 정확한 필드 오류로 차단(장부에 안 남음) → 데이터 관리에서 마감 전 거래 삭제 시도 시 정확한 토스트로 차단(콘솔의 `PERIOD_CLOSED` 로그로 이중 확인, 거래 그대로 존재) → 마감 해제 → 같은 달에 새 거래 저장·삭제 모두 다시 성공. 앱 JS 에러 0(의도된 진단 로그 제외). 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.36 | 가계부 여러 개 관리(다중 렛저). 배경: 스크린샷("우즈벡 유학생활" 개인 가계부 설정 화면) + "가계부 영구마감·마감해제 추가하는 건 어때? 그리고 새로운 가계부를 추가하고... 당신 생각은?" → 조사 결과 마감은 표시 전용(실제 잠금 없음)이고 가계부 추가는 앱이 "가계부 1개"를 전제로 짜여 있어 마감보다 훨씬 큰 구조 변경임을 확인 → 필자 비판적 검토(영구마감+마감해제는 이름·동작 불일치, 마감을 먼저 만들면 나중에 가계부가 여러 개 생겼을 때 범위를 다시 손봐야 함) → "가계부 추가 → 마감" 순서 제안 → 사용자 승인. **근본 상태**: `AppService.reload()`가 항상 첫 번째 가계부만 골라 씀(전환 UI 없음), DB엔 이미 유니크 제약이 없어 마이그레이션 불필요 — 앱 레이어만 열면 되는 구조. **설계**: 활성 가계부는 로컬 설정(`config.activeBusinessId`, 0.33 `ledgerMode`와 같은 패턴, 기기마다 다를 수 있음). 필터링은 `reload()` 단일 지점에서만(accounts/transactions/counterparties/journals/journalLines/evidenceFiles/auditLogs/closings 전부 활성 가계부 id로 거름) — 25개 넘는 화면 렌더러는 한 줄도 안 고침. 모드도 가계부별 분리(`config.ledgerModeByBusiness`, 레거시 단일설치는 전역값 폴백 후 다음 저장 시 그 id로 못박힘). **구현**: `state.businesses`(전체 목록), 순수함수 `pickActiveBusiness(businesses, configuredId)`(설정값 우선→목록 첫번째→null, 로직 테스트로 잠금), `AppService.createLedger({name,mode})`(완전히 새 businessId로 사업자+사업장+기간설정+계정과목 세트 생성 후 활성 전환, 기존 가계부 무변경), `AppService.switchLedger(businessId)`, `isPersonalMode()` 가계부별 재작성, 설정에 `renderLedgerManagement()`(목록+상태배지+전환+새 가계부 추가 폼) 신설. **검증**: 로직 테스트 +4(총 96, pickActiveBusiness 4분기). 헤드리스 Chromium: 가계부1(우즈벡 유학생활, 식비 5,000원) → 설정에서 가계부2(한국 생활비) 추가(즉시 활성 전환, topbar·배지 확인) → 가계부2는 거래 0건(격리) → 교통비 2,000원 저장 → 장부에 가계부2 거래만 보이고 가계부1 거래는 안 보임 → "전환"으로 가계부1 복귀 → topbar 복원, 가계부1 거래만 보이고 가계부2 거래는 안 보임(양방향 격리 확인). 앱 JS 에러 0. **남은 위험**: 가계부 삭제 기능 없음(후속), 마감(영구마감+마감해제)은 다음 순서로 이월, 사업장 복수화는 미지원(가계부 복수화와 다른 개념), 전환 UI는 설정 화면에만 있음. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.35 | 기기간 자동 동기화(기본값). 배경: 0.34 배포 보고에서 "개인 가계부 모드가 다른 기기 첫 로그인 시 기기 간 미동기화로 잠깐 모드 선택 화면이 다시 뜰 수 있음"을 남은 위험으로 보고 → 사용자 "기기간 실시간 동기화가 기본으로 가게 해야하는데 방법을 찾아보자" → 1단계(자동 동기화 기본값, 기존 로직 재사용·안전) vs 2단계(Supabase Realtime push, 진짜 실시간·복잡도↑) 제안 → 사용자 "1단계 진행하자". 근본 원인: `init()`이 세션은 확인하지만 `syncNow()`를 자동 호출하지 않고, `online` 리스너는 토스트만 띄우고 실제 동기화를 안 함 — 100% 수동(버튼 클릭)이었음. **구현**: `SyncService.scheduleAutoSync(delayMs)`/`runAutoSync()`(디바운스 스케줄러, 기존 `connectAndSync` 재사용) + 5개 트리거(부팅 시 세션 있으면 즉시, `window focus`, `document visibilitychange`, `online`, 30초 주기 `setInterval`). **안전장치**: 거래 입력·설정·가져오기 화면은 입력 중일 수 있어 백그라운드 동기화 후 전체 재렌더를 건너뜀 — `AUTO_SYNC_UNSAFE_ROUTES` 상수로 이름을 붙이고 로직 테스트로 회귀 잠금(이 목록이 줄면 입력 중 값이 지워질 수 있음). **아키텍처 결정**: 로컬 저장 직후 즉시 push하려면 `AppService`의 모든 변경 메서드(~10곳)에 훅을 걸어야 해 계층 분리가 흐려지고 봉합점 누락 위험이 있어, 대신 `SyncService` 자체 트리거만 늘리는 쪽을 택함(최대 30초 지연을 감수). 데이터 관리·대시보드 카피를 "자동으로 동기화됩니다" 톤으로 갱신. **검증**: 로직 테스트 +1(총 92, `AUTO_SYNC_UNSAFE_ROUTES` 내용 잠금), VM 샌드박스에 `setInterval`/`clearInterval` 추가(없으면 앱 로드 자체가 깨짐, 실제로 재현 후 수정). 헤드리스 Chromium: focus·visibilitychange·online 이벤트 강제 발생 + 디바운스 대기 후 콘솔 에러 0(비로그인 상태 안전 무시), 데이터 관리 화면 신규 카피 확인. **정직한 한계**: 실제 두 기기 동시 로그인 왕복은 Google 로그인이 필요해 헤드리스 불가 — `docs/accounting-ledger-browser-checklist.md`에 "5b. 기기간 자동 동기화 — 기본값 (0.35)" 수동 확인 섹션 신설. 진짜 실시간이 아니라 최대 30초(또는 이벤트 발생 시점까지) 지연 가능, Supabase Realtime(2단계)은 후속. 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.34 | 거래처 소프트삭제 + Excel 읽기 기능탐지(사용자 제공 36개 개발 규율 프롬프트 감사). 사용자가 이전 Medical Note 앱에서 뽑은 규율 프롬프트 문서를 이 앱에 전수 대조 — 20개 이상은 이미 CLAUDE.md·하네스 게이트에 반영돼 있었고, 신규 적용 대상 4건 중 2건을 이번 릴리스에 반영. **① 거래처 소프트삭제**: `AppService.deactivateCounterparty`를 `deactivateAccount`와 동일 패턴(soft-delete+tombstone+audit+queue)으로 신설(스키마엔 `counterparties.deleted_at`이 이미 있었음 — 앱 레이어 gap만 해소). 설정에 `거래처 관리` 패널(목록+비활성화, 생성 폼 없음). 데이터 생명주기 매트릭스의 counterparties 삭제→tombstone ✗→✓ 갱신. **② DecompressionStream 기능탐지**: `XlsxReader.text()`에 `typeof DecompressionStream==='undefined'` 가드 추가, 미지원 브라우저에서 원인불명 크래시 대신 한국어 안내로 치환. **③ 반응형 오버플로 실측 감사(코드 변경 없음)**: 헤드리스 Chromium으로 375/768/1280 3개 뷰포트×10개 화면 조합에서 `document`/`body` scrollWidth-clientWidth 실측, 전부 0(가로 overflow 없음) 확인 — 누적된 "수동 확인 필요" 체크리스트 항목을 실측으로 검증. 헤드리스 전체 시나리오(거래 입력→거래처 자동생성→관리 패널 노출→비활성화→목록 제외) 검증, 로직 테스트 91 유지, 마이그레이션 없음(순수 앱 레이어) |
| 거버넌스(앱 버전 무변경) | **④ 개념 정의 원장 + anchor-existence 게이트**(36개 프롬프트 감사 4번째 항목, 같은 세션에서 바로 이어 구현). `docs/accounting-ledger-concept-ledger.md` 신설 — SSOT·소프트삭제+tombstone·LWW·빈클라우드가드·결정적ID·canonical_version·RLS·버전계약·데이터생명주기매트릭스·용어대장·게이트우선개발·정직한완료구분 등 13개 저장소 고유 개념을 정의/규칙(왜)/집행(앵커)/자세히 4칸으로 정리. 하네스 `concept-ledger-contract` 게이트가 각 "집행" 앵커(백틱 토큰)가 저장소의 다른 파일에 실존하는지 검증(문서·코드 드리프트 방지). **구현 중 자기참조 버그 1건 발견·차단**: 첫 구현이 검색 대상(haystack)에 원장 문서 자신을 포함시켜, 앵커를 오타로 바꿔도 그 오타 자체가 문서 안에 있어 "존재한다"고 오판(게이트가 항상 통과하는 무력한 상태) — haystack에서 원장 파일 자신을 제외해 수정, 오타 주입→FAIL·복구→PASS 양방향 검증으로 확인 후 커밋. `index.html` 미수정이라 버전 무변경(CLAUDE.md 버전 규칙: 문서·도구만 바꾸면 올리지 않음), `project-contract` requiredFiles 15개로 갱신 |
| 앱 0.33 | 개인 가계부 모드. 사용자 논의(개인 지출도 앱으로 관리하고 싶다 → 복식부기 아니라 간편장부처럼 → 첫 화면에서 모드 선택 + 계정과목 확장 가능 구조)를 반영. **① 첫 실행 모드 선택**: `!state.business && !state.config.ledgerMode`일 때 `renderModeSelect()`(개인 가계부/사업자 카드 2개) 게이트, 선택 시 `state.config.ledgerMode`(localStorage, 스키마 무변경) 저장 후 설정으로 이동. **② 계정 템플릿 재구성**: 기존 `ACCOUNT_TEMPLATES`(15개, 무변경)에서 `accountMap()`/`buildPosting`이 항상 참조하는 "배관용" 6개(현금·보통예금·외상매출금·부가세대급금·미지급금·부가세예수금)를 `CORE_ACCOUNT_TEMPLATES`로 파생(필터, 중복 정의 아님). 신규 `PERSONAL_ACCOUNT_TEMPLATES`(18개: 급여/용돈 등 수입 3·식비 등 비용 12·예금적금 등 자산 3, 코드 P접두). `setupBusiness`가 모드별로 `CORE+PERSONAL` 또는 기존 `ACCOUNT_TEMPLATES`를 시딩 — **CORE를 항상 함께 시딩하지 않으면 개인 모드에서 `accountMap()`이 bank/receivable 등을 못 찾아 모든 거래 저장이 크래시하는 잠재 버그**를 설계 단계에서 발견·차단. **③ accountChoices 일반화**(버그 수정): 자산구입 선택지가 `local_key==='equipment'`로 하드코딩되어 있던 것을 `account_type==='asset' && !CORE_ACCOUNT_KEYS.includes(local_key)`로 일반화 — 사업용 비품뿐 아니라 개인 자산(예금/적금 등)과 사용자 추가 자산 계정도 선택 가능해짐(기존 비품 단일 동작은 회귀 없이 유지, 헤드리스로 확인). **④ 계정과목 CRUD**(모드 공통 확장 지점): `AppService.createAccount`/`deactivateAccount`(기존 `deleteTransaction`과 동일한 soft-delete+tombstone+audit+queue 패턴), 설정에 `계정과목 관리` 패널(그룹별 목록+비활성화 버튼+추가 폼). 데이터 생명주기 매트릭스의 `accounts` 삭제→tombstone 열을 ⊘→✓로 갱신(실제 배선됨을 반영). **⑤ 화면 축소**: 개인 모드에서 사업자등록번호·업종코드·과세유형·사업장 주소 필드 hidden(DOM엔 남아 el() 참조 안전, 제출 핸들러 무변경), `PERSONAL_HIDDEN_NAV`(전표검토·가져오기·마감)로 네비게이션 필터, `PERSONAL_HIDDEN_GUIDE_TOPICS`(기장의무·부가세면세·추계경비율·해외구매)로 가이드 필터, 리포트의 추계계산·법정서식 섹션 숨김(요약 카드는 유지), 거래폼 부가세 칸 숨김(기본값 `none`), `overseasNoteHtml`에 개인용 문구 분기, 대시보드 과세유형 확인 태스크 숨김. 설정에 `개인 가계부/사업자` 전환 토글(데이터 유지, 즉시 반영). **검증**: 로직 테스트 +7(템플릿 개수·타입 분포·코드 접두어·accountChoices 배관용 제외, 총 91). **헤드리스 Chromium 전체 플로우**: 모드선택→가계부 저장(계정 24개 시딩 확인)→식비 30,000원 저장(차변=대변 균형 확인, 크래시 없음)→자산구입 드롭다운(예금/적금 등 3개, 현금/보통예금 제외)→계정과목 추가(반려동물)·즉시 선택 가능·비활성화 후 제외→네비게이션(9개, 전표/가져오기/마감 없음)·가이드(3개 토픽만) 확인→모드 전환(사업자, 12개 메뉴 복원). **회귀 헤드리스**: 사업자 모드 처음부터 진행 시 계정 15개(무변경)·비용 6종(무변경)·자산 비품만(무변경)·과세거래 11,000원 균형·리포트 추계섹션 노출 전부 기존과 동일 확인. 앱 JS 에러 0(전 시나리오). 마이그레이션 없음(순수 앱 레이어) |
| 앱 0.32 | 계정과목 선택 가이드. SSOT 중복 없이 `SimpleBookAccounts.GROUPS`(국세청 25개 계정과목, 기존 name/desc 유지)에 `kid`(눈높이 설명)·`examples`(실제 예시 1~3개) 필드만 추가. 가이드 새 토픽 `account-guide`(`accountGuideHtml`/`accountCardHtml`) — 그룹별 카드(법적 정의+눈높이+예시) + "자주 헷갈리는 것" 구분법 박스(소모품비 vs 비품=1년 이상·고가 여부, 기업업무추진비 vs 복리후생비=거래처 vs 직원, 여비교통비 vs 차량유지비=이동 vs 차량 자체). 거래 입력 화면 계정과목 라벨 옆 `선택 가이드` 버튼(`data-open-guide-topic`, 기존 범용 핸들러 재사용, 신규 JS 없음) → 가이드로 즉시 이동. `.term-card-examples`·`.guide-link` CSS. **헤드리스 검증**: 가이드 7토픽·25개 계정카드·구분법 문구·예시 표시, 거래폼 바로가기 클릭→가이드 이동·25카드 렌더. 앱 JS 에러 0. 로직 테스트 +2(전 계정 kid·examples 완결성, 총 84). 마이그레이션 없음 |
| 앱 0.31 | 월별 사용현황. 사용자 스크린샷 요청(리포트 상단 우측 자리에 버튼) — 실제로는 전역 topbar(동기화 배지·거래입력 버튼 옆)에 `월별 사용현황` 버튼 추가(모든 화면에서 접근 가능). `AccountingDomain.monthlyAccountMatrix(transactions, accounts)`(순수 함수): 계정과목(열)×월(행) `total_amount` 합산, 행·열·총합계 계산. `renderMonthlyUsageModal()`이 `#modalRoot`에 연도 선택 + 스크롤 가능한 표(첫 열·머리글 고정, 합계 열/행 강조) 렌더. 사업자 미설정 시 오류 toast만(모달 안 열림). 계정과목 이름은 `state.accounts`를 그대로 써서 **간편장부·복식부기 어느 관점이든 하나의 표로 커버**(별도 분기 없음). `.usage-table` CSS. **헤드리스 검증**: 무사업자 시 모달 안 열림, 실제 거래 3건(1월 2건·2월 1건) 입력 후 모달에서 1월 1,500원·2월 7,000원·합계 8,500원 정확. 로직 테스트 +6(총 82). 마이그레이션 없음 |
| 앱 0.30 | 해외 거래 통화 선택 목록. `CURRENCIES`(ISO 4217, 기축·주요 통화 USD/EUR/JPY/GBP/CNY/CHF… + 사용자 요청 **UZS 우즈베키스탄 숨** + 지역 통화 KZT/RUB/VND/THB… 28개). 통화 입력칸을 `datalist`(`#fxCurrencyList`) 자동완성으로 — 목록에서 고르거나 3글자 코드 직접 입력(목록 밖 통화도 허용). 헤드리스 검증(28개 옵션·UZS 존재·datalist 연결). 로직 테스트 +3(UZS 포함·기축통화 포함·3글자 ISO, 총 76). 마이그레이션 없음 |
| 앱 0.29 | 해외 거래 구조화 저장(스키마 확장). **마이그레이션** `20260712000500_accounting_v1_overseas_fields.sql`: `source_transactions`에 `is_overseas`(bool, default false)·`foreign_currency`(text)·`foreign_amount`(numeric)·`exchange_rate`(numeric) 추가 — additive·nullable·RLS 무변경. **프로덕션 적용 완료**(하드룰: BEFORE 조회=대상 컬럼 0·테이블 0행 → apply_migration → read-back 4컬럼 확인, `last_schema_version` 0.03→0.04). 앱: `saveTransaction`이 구조화 필드 저장(0.28의 내용 부기 제거), 장부에 `🌐 해외` 배지(마우스오버 통화·환율). 동기화는 전체 row 업로드라 새 컬럼 자동 반영. 하네스 `expectedMigrations` 5개로 갱신. **헤드리스 검증**: 해외 비용 저장→is_overseas=true·USD·100·1350·total 135,000·배지. 로직 테스트 73 유지 |
| 앱 0.28 | 거래 입력 해외 거래 연동. 거래 폼에 `해외 거래` 체크박스 + `#overseasBox`(통화·외화 금액·환율). `AccountingDomain.fxToKrw(외화, 환율)`=round(외화×환율)로 원화를 계산해 `#totalAmount`에 자동 채움(기존 calculateAmounts→save 경로 그대로, 전기 엔진 무변경). `overseasNoteHtml()`이 거래유형·과세유형에 따라 안내 — **면세사업자(vatPayerType==='exempt') 비용/자산 시 대리납부 danger 경고**(§52), 그 외 수입부가세/용역 안내. 저장 시 내용에 `· 해외 USD 100@1350` 부기(스키마 무변경). **헤드리스 검증**: 토글 표시·100×1,350→135,000원·면세 대리납부 경고. 로직 테스트 +2(fxToKrw, 총 73). 마이그레이션 없음 |
| 앱 0.27 | 해외 구매·수입 처리 안내. 법령 MCP로 부가가치세법 §4(과세대상: 재화의 수입)·§52(대리납부) 원문 확인 후 가이드 `overseas` 토픽 추가 — ①해외 물품 수입(수입부가세, 과세사업자 매입세액 공제)·②해외 용역(§52 대리납부, **면세사업자·비사업자 10% 의무** 경고)·③해외 현지 소비(부가세 없음), 환율(기준환율)·증빙 안내 + 눈높이 설명. 용어 3개(수입부가세 §4②·대리납부 §52·기준환율) `TAX_TERMS`+용어대장에 추가(양방향 게이트 31↔31). legal-basis SSOT에 §8 해외 거래 절 추가. 헤드리스 검증(토픽 렌더·대리납부 경고·검색). 로직 테스트 +2(총 71). 마이그레이션 없음 |
| 앱 0.26 | 가져오기 검증·수정 모달(제안+승인). `SimpleBookImport.validate/suggestAccount/remapAccount/recomputeVatInclusive`(순수, 값 미변경 탐지). 오류: bad_date(월>12·일>31 실검증)·no_amount·negative. 경고: unknown_account(+가장 가까운 계정과목 제안)·ambiguous_kind·vat suspect(부가세≈금액÷11이면 총액 오입력 의심). 모달(`#modalRoot` 기존 CSS 활용, `renderImportReview`/`bindReviewEvents`)이 업로드 시 자동 오픈, 미분류 계정과목 드롭다운 치환·부가세 일괄 재계산(공급가액=금액−부가세)을 사용자 승인 후 적용, 오류 행은 반영에서 자동 제외. **무손실 정규화만 자동, 값 변경은 승인**. **실 샘플 헤드리스 검증**: 부가세 의심 512건 탐지→일괄적용→경고 0→945건 균형 반영(차변=대변). 로직 테스트 +6(총 70). 마이그레이션 없음 |
| 앱 0.25 | 간편장부 가져오기 → 복식부기 원장 전기(#5 완성). `AppService.importSimpleBook(rows)`: 각 행을 기존 `buildPosting`/`validateJournal` 엔진으로 균형 전표화. **결정적 id**(`Utils.deterministicId` = cyrb128→UUID v5형)를 `businessId+sourceRow+내용`에서 파생 → 같은 파일 재반영 시 IndexedDB put 덮어씀(중복/좀비 방지), 내용 동일·행 다르면 구분. 없는 계정과목은 `SimpleBookAccounts` 그룹으로 type 판정해 자동 생성(장부 view가 원 명칭 표시), 거래처도 자동 생성. `source_channel='simple_book_import'`, 미분류 계정과목은 `requires_review`. 단일 putMany+큐, reload 1회. UI: 미리보기 요약에 `장부에 반영` 버튼(확인 후 전기→장부 이동). **실 샘플 945건 헤드리스 검증**: 945 거래·2,427 라인·차변=대변 81,651,988(전부 균형)·계정 20개(+6 자동), 재반영 시 945 유지(멱등), 앱 JS 에러 0. 로직 테스트 +3(deterministicId, 총 64). 마이그레이션 없음(스키마 무변경) |
| 앱 0.24 | 간편장부 Excel 미리보기(#5 파싱+미리보기). `XlsxReader`(ZIP 중앙디렉터리 파싱 + `DecompressionStream('deflate-raw')`, 무 CDN) + `SimpleBookImport`(「장부」 시트 파서). 컬럼: **A=월·B=일**(합쳐 YYYY-MM-DD)·C=계정과목·D=거래내용·E=거래처·F/G=수입·H/I=비용·J/K=자산·L=비고, M열 이후 사용자 보조열 무시. 금액열로 income/expense/asset 분류, `SimpleBookAccounts`로 계정과목 존재 확인(미분류 표기). `renderImports`에 파일 입력+요약+미리보기표(상위 200건, 원본 보존, **자동 전기 없음**). `.import-preview` CSS. **실 사용자 샘플(a4968fab, 945건) 헤드리스 검증**: 2025년·수입 8/비용 937·합계 수입 40,000,000·비용 38,625,037, 첫 행 2025-01-01 여비교통비 1,450 정확, 앱 JS 에러 0. 로직 테스트 +5(buildDate·classify·normalizeRow, 총 61). 마이그레이션 없음 |
| 앱 0.23 | 용어 대장 강제 + 툴팁 단일화 + SSOT 정합 게이트. **① 용어 대장**: `docs/accounting-ledger-term-ledger.md` 신설(28개 용어+근거), 하네스 게이트 `term-ledger-contract`가 `TAX_TERMS`↔대장 **양방향 일치**와 각 용어의 `law`·`kid` **필수**를 강제(컨센트 대장 개념). **② SSOT 정합**: `legal-ssot-contract` 게이트가 `BookkeepingDuty`(3억/1.5억/7,500만)·`ExpenseRateMethod`(6,000만/3,600만/2,400만) 임계값을 코드에 고정하고 legal-basis 문서의 금액 표기와 대조. **③ 툴팁 단일화**: `TERM_HELP`에서 세법 용어(복식부기·차변·대변·공급가액·계정과목) 제거→앱 고유 용어만 유지, `showTerm`이 `TermService` 우선 사용(중복 정의 제거). 새 필수파일 2개(term-ledger, legal-basis-reference) requiredFiles 등록. **게이트 양방향 검증**: 유령용어/빈 law/임계 변경 모두 FAIL 확인, 복구 시 PASS. 로직 테스트 56 유지. 마이그레이션 없음 |
| 앱 0.22 | 세법 용어사전 + 대시보드 검색. `TAX_TERMS`(28개 용어) + `TermService`(all/find/search) 추가 — 용어마다 법적 정의(근거 조문: §160·§208·§143·§80·§26·§14·§24·§27·§19·§29·§37 등)와 초등학생 눈높이 설명. 가이드에 `주요 세법 용어사전` 토픽(5개 분류로 그룹, `termsGuideHtml`/`termCardHtml`), 대시보드에 `세법 용어 검색`(input→TermService.search→카드) + `전체 용어 보기`(data-open-guide-topic로 가이드 이동). `.term-*` CSS. 헤드리스로 검색·이동·28개 카드 렌더 검증, 앱 JS 에러 0. 로직 테스트 +6(총 56). 마이그레이션 없음 |
| 앱 0.21 | 법령 가이드 눈높이 설명 + 법령 기준 SSOT 스킬 신설. **① 눈높이**: 가이드 3개 법령 토픽(기장의무·면세·추계)에 `왜 이렇게 정했을까요?` 블록(`.guide-why` CSS) 추가 — 각 제도의 목적·취지를 중학생 눈높이로 설명. **② 스킬 SSOT**: 새 문서 `docs/skills/accounting-legal-basis-reference-skill.md`(`Sub_legal-basis-reference_0.01`)에 기장의무(§160·§208⑤)·단순/기준경비율 적용(§143④)·추계 계산(§143③, 작성사례 552101 검증표)·부가세 면세(§26① 20개 호)·경비율/업종코드/간편장부 계정과목 기준을 조문·값·출처·목적·앱 서비스 매핑과 함께 정리. 법령 MST/대조일 기록. **③ 연결**: income-tax 스킬 0.04·tax-vat 스킬 0.05가 이 SSOT를 가리키도록 갱신, CLAUDE.md 도메인 스킬 순서에 SSOT 포인터 추가. 헤드리스로 눈높이 3개 렌더 검증, 앱 JS 에러 0. 로직 테스트 50 유지. 마이그레이션 없음 |
| 앱 0.20 | 계산기 고도화 + 법령 가이드. **① 계산기**: 추계 계산기에 `직전연도 수입금액` 입력 추가 → `BookkeepingDuty.assess`로 기장의무(복식/간편) 자동판정 + estDuty select 자동 선택, 신규 `ExpenseRateMethod`(소득세법 시행령 §143④2호: 가 6,000만·나 3,600만·다 2,400만, 인적용역 94xxx→나목)로 단순/기준경비율 자동판정. **② 가이드**: `renderGuide`를 GUIDE_TOPICS 레지스트리로 리팩터(google-cloud 상세는 `googleCloudGuideHtml`로 분리), `legalGuideHtml`로 기장의무(소득세법 §160·시행령 §208⑤)·부가세 면세(부가가치세법 §26①)·추계 경비율(소득세법 §80③·시행령 §143③④) 안내 버튼 3개 추가. 모든 조문은 법령 MCP로 원문 대조 확인. `.legal-basis`·`.est-tag` CSS. **검증**: 헤드리스 Chromium으로 가이드 4개 토픽 근거 렌더 + 계산기 자동판정 서비스(복식/간편·기준/단순) + 소득 21,220,000 재확인, 앱 JS 에러 0. 로직 테스트 +6(총 50). 마이그레이션 없음 |
| 앱 0.19 | 기준경비율 추계소득 **계산기 UI**(②그룹 #5 완성). 리포트에 `estimatorPanelHtml` 계산기 추가 — 총수입·매입·임차·인건비 입력 + 기장의무(복식/간편) 선택, 활성 사업자 업종코드의 기준·단순경비율 자동 채움(수정 가능), `bindViewEvents`에서 input/change로 `EstimatedIncome.byStandardRate()` 실시간 호출해 주요경비·①·②·소득금액(min)·필요경비 표시. `.estimator*` CSS(반응형 grid, `--brand`). **헤드리스 Chromium(playwright-core, /opt/pw-browsers)으로 작성사례 수치 왕복 검증**: 복식부기 → ①21,220,000·②42,024,000·소득 21,220,000·필요경비 98,780,000, 간편장부 → ②34,608,000·소득 15,400,000. 앱 JS 콘솔 에러 0(외부 CDN 차단 제외). 순수 로직 불변(테스트 44 유지). 마이그레이션 없음 |
| 앱 0.18 | 기준경비율 추계소득 계산 엔진·안내(②그룹 #5). 사용자 제공 「추계신고_기준경비율 작성사례(복식부기의무자 한식점 552101)」 PDF의 공식 계산식을 `EstimatedIncome.byStandardRate()`로 구현. 소득금액 = min(① 기준소득 = 수입−주요경비−수입×기준경비율×(복식부기 ½), ② 비교소득 = 수입×(1−단순경비율)×(복식부기 3.4배/간편 2.8배)). 로직 테스트를 **작성사례 실제 수치**(120,000,000→기준 21,220,000·비교 42,024,000·필요경비 98,780,000)로 고정. 부수 교차검증: 앱 0.15 경비율 데이터의 552101(단순 89.7·기준 9.7)이 작성사례와 정확 일치. 리포트에 계산식·정규증빙·복식부기의무자 추계 가산세 경고를 **검토용**으로 안내(확정 계산 UI는 아직 아님). 로직 테스트 +4(총 44). 마이그레이션 없음 |
| 앱 0.17 | 간편장부 계정과목 분류표(②그룹 #4). 국세청 간편장부 작성요령 [참고] 계정과목 분류표(사용자 제공 인적용역 작성사례 p.6)를 `SimpleBookAccounts` 서비스로 임베드 — 수입 2·비용 16·제조비용 3·자산 4 = 25개 계정과목과 공식 설명, 간편장부 열 매핑(수입→수입금액, 비용·제조비용→비용, 자산→자산). `columnOf(name)`로 계정과목→열 조회(향후 Excel import 매핑 기준). 복식부기 내부 COA(101/401/811…)와 별개인 **간편장부 view/import 계층**. `renderImports`(가져오기)에 분류표 표시. 이미지 원본 미추적. 로직 테스트 +6(총 40). 마이그레이션 없음 |
| 앱 0.16 | 기장의무·부가세 면세 근거 자동 표시(②그룹 #2·#3). 법령 MCP로 **소득세법 시행령 제208조 제5항**(복식부기/간편장부 기준: 가목 3억·나목 1.5억·다목 7,500만원)과 **부가가치세법 제26조 제1항**(면세 20개 호) 원문을 조회해 SSOT로 삼음. `BookkeepingDuty`(NTS 대분류 index→가/나/다 그룹→기준금액, `assess(code, 직전수입, 신규여부)`→복식부기/간편장부 판정)와 `VatExemption`(대분류·94접두→면세 후보 호+예외 caveat) 서비스 추가. `industryPickedHtml`이 업종코드 선택 시 기장의무 기준금액과 면세 가능성을 **근거 조문과 함께 후보로** 표시. 면세는 공급 성질로 판단하므로 "개별 판단" 명시(하드룰: 확정 표시 금지). 로직 테스트 +10(기장의무 7건·면세 3건, 총 34). 마이그레이션 없음 |
| 앱 0.15 | 경비율 자동 표시(②그룹 #1). 사용자 제공 「국세청 2025년 귀속 단순·기준경비율」 xlsx(1,542개)를 `scripts/build-expense-rates.py`로 파싱해 `NTS_EXPENSE_RATES`(code→[단순일반,단순초과,기준일반], 38KB) 파생 상수로 임베드. `ExpenseRates.find(code)`로 조회. 업종코드 선택 시 `industryPickedHtml`이 단순·기준경비율(초과율 포함)을 **후보**로 표시(귀속연도·출처 명시). 초과율 적용은 수입 규모 임계에 따라 달라 값 표시만 하고 자동 계산은 안 함. 원본 xlsx 미추적. 로직 테스트 +3(741400→77.3/23.1, 940903 초과 46.4 등, 총 24). 마이그레이션 없음 |
| 최근 기준 커밋 | `d9d5a75 feat: property tax co-ownership share + real-bill validation (app 0.43)`(0.44 계산과정 설명+공시가격 입력 안내는 이 커밋 위에 작업 중, 아직 커밋 전). main = origin/main = `d9d5a75`(0.43까지 전부 반영, 배포 완료; 0.44는 브랜치) |

## 다음 구현 우선순위

다음 사용자 영향 변경은 앱 `0.06`이며 아래 순서로 진행한다.

1. (완료 · 0.04) 인증 사용자 기준 `businesses` CRUD와 RLS 왕복 검증
2. (완료 · 0.07) 비허용 Google 계정 차단과 owner 허용 사용자 관리 흐름 검증
3. (완료 · 0.08) 일반 동기화와 canonical version 변경 수렴의 다기기 자동 테스트 (0.06 tombstone 삭제 수렴 + 0.08 canonical 최종본 지정 배선·자동 테스트 9/9. 실브라우저 2대 왕복은 수동 체크리스트로 남음)
4. (완료 · 0.09) Cloudinary 이미지/PDF 업로드와 증빙 파일 메타 연결 (첨부·조회·동기화 구현. 증빙 삭제/교체와 Cloudinary 원본 삭제(서명 필요)는 후속)
5. 국세청 간편장부 Excel import 미리보기·원본 행 보존·확정 흐름
6. 거래 수정·마감 후 변경 통제와 감사로그 고도화 (0.05에서 거래 부분 소프트삭제 구현)
7. 법정서식 스냅샷과 리포트 필드 매핑

0.04에서 로그인한 owner 세션의 access token으로 `businesses` INSERT·SELECT·UPDATE·soft delete·DELETE를 실제 REST로 왕복하는 자가검증을 구현했다. Supabase `businesses` 정책은 SELECT `owner_user_id = auth.uid() and deleted_at is null`, INSERT `owner_user_id = auth.uid() and accounting_is_allowed_user()`, UPDATE·DELETE `owner_user_id = auth.uid()`로 확인했고(RLS 4/4), owner allowlist는 `hanwha27@gmail.com:owner:active`, canonical_version은 `0`이다. owner uid `c9ff5188-51a7-4c01-b653-b6e1d73d0790`로 시뮬레이션한 인증 세션에서 create·read·update 왕복이 통과했으며, soft delete 격리와 정리는 확인된 SELECT/DELETE 정책의 직접 결과다. 실제 브라우저 owner 로그인 왕복은 수동 체크리스트로 남는다. Cloudinary, Excel, 법정서식 출력은 아직 완료 기능이 아니다.

0.05에서 `데이터 관리` 화면을 추가해 저장 상태 확인, JSON 백업·복원, 클라우드 동기화, 거래 부분 삭제, 이 기기 전체 삭제를 2열 대칭 그리드로 통합했다. 거래 부분 삭제는 원천거래와 그 전표·전표라인을 함께 `deleted_at` 소프트삭제하고 `tombstones`·`audit_logs`·`sync_queue`에 반영한다. 일괄 삭제는 IndexedDB store만 비우고 localStorage의 Supabase 연결 설정·기기 식별자는 유지한다.

0.06에서 다기기 삭제 수렴을 구현했다. `SyncService.convergeTombstones`가 매 동기화에서 cloud tombstone을 pull, cloud에 없는 로컬 tombstone을 push(`resolution=ignore-duplicates`로 append-only 안전), 모든 tombstone을 해당 store의 로컬 행에 멱등 소프트삭제로 적용한다. `businesses` 등 SELECT RLS가 `deleted_at is null`로 삭제 행을 숨겨 pull로는 전파되지 않던 삭제가 tombstone 채널로 모든 기기에 수렴한다. `tombstones` 테이블은 이미 존재(컬럼 8개, RLS SELECT/INSERT = `(business_id is null and accounting_is_allowed_user()) or accounting_can_access_business(business_id)`, UPDATE/DELETE 정책 없음=append-only)해 마이그레이션이 없었다. 좀비 데이터 방지 근거: (a) 삭제 행은 `updated_at` 최신값 승리 병합에서 활성 복사본에 덮이지 않고, (b) 동기화는 `sync_queue` 항목만 업로드해 비-큐 로컬 행을 재업로드하지 않으며, (c) 삭제 전파는 tombstone으로만 이뤄진다. 로직 테스트 7/7(다기기 수렴·멱등성·재생성 없음·미지 store 안전), owner 인증 tombstone INSERT/SELECT DB 검증(롤백)을 통과했다.

남은 잠재 좀비 트랩: 향후 거래 **수정(update)** 기능을 추가하면, 다른 기기에서 이미 삭제됐지만 아직 tombstone을 못 받은 행을 편집·재업로드해 되살릴 수 있다. 수정 기능 구현 시 upsert 전에 `tombstones`/`deleted_at`를 확인해 tombstoned id는 되살리지 않도록 가드해야 한다. 현재는 거래 수정 UI가 없어 트리거되지 않는다.

0.07에서 소유자 전용 허용 사용자 관리 흐름을 구현했다. `SupabaseAdapter`의 `listAllowedUsers`/`insertAllowedUser`/`updateAllowedUser`/`logAccessEvent`와 `SyncService`의 `loadAllowedUsers`/`addAllowedUser`/`setAllowedUserStatus`로, 설정의 owner 전용 패널에서 `app_allowed_users`를 조회·추가·차단·재허용한다. `app_allowed_users` CHECK 제약(role `owner/editor/viewer`, status `active/blocked/pending`)에 앱 값을 맞췄다(이전 초안의 `member`/`revoked`는 제약 위반이라 수정). DB 검증(모두 롤백): owner 인증 세션에서 insert(viewer/active)·update(blocked)·`auth_access_logs` insert·전체 조회(2행) 통과. 비-owner(`intruder@example.com`) 세션은 `accounting_is_bootstrap_owner()=false`, allowlist 가시 행 0, insert는 RLS 42501로 차단됨을 확인했다. 소유자 자기 차단은 `ALLOWLIST_OWNER_PROTECTED` 가드로 막는다. 마이그레이션 없음. `auth_access_logs` 컬럼: id(default)·actor_user_id·actor_email·action·target_email·result·detail·created_at.

0.08에서 canonical version 최종본 지정을 배선했다. 지금까지 cloud `canonical_version`을 아무도 올리지 않아 잠들어 있던 수렴 소비 경로가, owner의 `designateCanonical`(전체 로컬+tombstone 업로드 후 `accounting_sync_meta.canonical_version` +1)로 활성화됐다. `accounting_sync_meta`는 SELECT=allowed user, INSERT/UPDATE/DELETE=bootstrap owner 전용. 다기기 시뮬레이션 자동 테스트 9/9(일반 병합 수렴, tombstone 삭제 수렴·무재생성, canonical 지정 시 소비 기기가 로컬 전용 변경을 버리고 수렴, 지정 기기가 삭제를 되살리지 않음)와 owner canonical upsert DB 검증(롤백, 실제 canonical은 0 유지)을 통과했다.

0.09에서 Cloudinary 증빙 첨부를 구현했다. 하드룰(secret 금지)에 따라 브라우저에서 제한된 unsigned upload preset으로만 업로드한다(`https://api.cloudinary.com/v1_1/{cloud}/{image|auto}/upload` + `upload_preset`). `evidence_files`(24열)와 `evidence_documents`는 기존 스키마·RLS(business-scoped)를 그대로 사용해 마이그레이션이 없었고, IDB는 버전 1→2로 `evidence_files` store를 추가(추가형, 기존 데이터 보존)했다. 로직 테스트 15/15(썸네일 URL 변환, 업로드 요청 구성·resource_type, upload_preset 전송, secret 차단, 메타 구성·감사 public_id 보관), owner `evidence_files` insert/read DB 검증(롤백)을 통과했다. 실제 파일 업로드 왕복은 사용자의 Cloudinary cloud name·unsigned preset과 브라우저가 필요하므로 수동 체크리스트로 남는다. 남은 후속: 증빙 삭제·교체 UI, Cloudinary 원본 삭제(서명 API=secret 필요, 브라우저 직접 불가 → Edge Function 후보), evidence_documents 그룹핑, 파일 해시.

0.10에서 다른 앱의 교훈 6건을 우리 앱에 대조·반영했다. 크리티컬 발견은 canonical replace 경로의 데이터 소실 위험(클라우드가 빈 응답을 주면 로컬 wipe)으로, 빈 클라우드 가드로 수정했다. 일반 merge 경로는 로컬을 Map 기반으로 유지해 이미 빈 응답에 안전하다. 도메인×생명주기 매트릭스로 gap을 가시화했고(가장 큰 gap: `evidence_files` 삭제→tombstone 미배선=증빙 제거 없음, `counterparties` 삭제 없음, import 미리보기 부재), SYNC_TABLE_ORDER 도메인이 매트릭스에 반드시 존재하도록 하네스 게이트를 추가했다. North Star 비타협 원칙을 설계지침 최상위에 명문화했다. SSOT 남은 손편집 중복: 새 동기화 테이블 추가 시 `SYNC_TABLE_ORDER`·IDB 인덱스·`reload()`를 각각 편집해야 함(매트릭스 게이트가 최소한 매트릭스 누락은 잡지만, 완전 자동생성은 후속 과제).

0.11에서 국세청 업종코드 입력을 자동화했다. 사용자가 제공한 국세청 「업종코드-11차 표준산업분류 연계표」(2023 귀속) Excel을 `scripts/build-industry-codes.py`로 파싱해 코드·업종명·대분류를 파생 상수(`NTS_INDUSTRY_CODES`, 1,784개)로 index.html에 임베드했다. 원본 Excel은 참고자료 하드룰에 따라 Git에 커밋하지 않고, 재생성 스크립트만 커밋했다(SSOT 교훈: Excel=출처, 스크립트=writer, 상수=파생물). 세무 정확성(North Star) 때문에 선택값은 확정이 아니라 후보(requires_review)로 표시하고 홈택스 공식 조회·경비율 고시 링크로 검증하도록 안내한다. 검색 로직 자동 테스트 7/7(컨설팅→741400, 의원→851201 등 실코드 반환). 임베드 크기 88KB로 index.html이 커졌으나 gzip으로 완화되고 오프라인 완전 동작한다.

0.12에서 생명주기 매트릭스가 지목한 `evidence_files` 삭제 gap을 형제 도메인 대칭으로 메웠다(증빙 제거 = soft-delete+tombstone+감사+큐, 다른 증빙 없으면 거래 미첨부 복귀). 제거 로직 자동 테스트 7/7.

0.13에서 다른 앱의 프롬프트 교훈 18건을 대조했다. 대부분(정독·SSOT·정직완료·프로덕션 증거·최소변경·게이트우선)은 이미 지켜온 규율이라 CLAUDE.md `AI 개발 규율`에 포인터로 흡수했고, 진짜 gap 하나 — 그동안 "N/N 통과"라고 보고한 로직 테스트가 전부 scratchpad(비커밋·비게이트)라 회귀를 못 막던 것 — 을 `scripts/tests/logic.test.mjs` 커밋 + `logic-tests` Required 게이트로 잠갔다(교훈: 특성화 테스트·게이트 우선·정직한 완료). 인적용역 요구(940600 등 사업자등록번호 없을 수 있음)는 UX 안내로 처리했다(데이터층은 기존에도 선택 지원). 남은 매트릭스 gap: counterparties 삭제(낮음), import 미리보기(중간). 다음 구조 후속 후보: 개념 정의 원장(집행 앵커) + 앵커 존재 게이트, 반응형 오버플로 헤드리스 게이트(chromium 사용 가능). 다음 기능은 국세청 간편장부 Excel import(#5, 백업 5봉합점 대칭)다.

0.14에서 입력 칸 자동화 검토를 시작했다. 사용자가 "법적 필요성 재검토 + 휴먼에러 방지 자동화"를 요청했고 자동화를 3그룹으로 분류했다. ①그룹(외부 자료 불필요)은 0.14 구현: 사업자번호 체크섬·대표자명 자동채움·큰업종 자동. ②그룹(사용자 자료 필요, 세무 정확성): ~~경비율 데이터~~(**0.15 완료**), ~~부가세 면세~~(**0.16 완료** — 부가가치세법 §26① 원문 근거, 업종 연결 호를 후보 표시), ~~기장의무 수입금액 기준표~~(**0.16 완료** — 소득세법 시행령 §208⑤ 원문 근거, NTS 대분류→가/나/다 그룹→기준금액), ~~간편장부 계정과목표~~(**0.17 완료** — 국세청 간편장부 작성요령 [참고] 계정과목 분류표 25개 임베드, `SimpleBookAccounts`. 단, 이 참고표에는 3자리 숫자 코드가 없음 — 숫자 코드가 필요하면 코드가 표기된 간편장부 서식/코드표를 받아야 함), ~~추계소득 계산식~~(**0.18 완료** — 국세청 추계신고(기준경비율) 작성사례 기준 `EstimatedIncome` 엔진, 작성사례 수치로 테스트 고정). 남은 자료 의존 항목: 경비율 **단순경비율 일반율↔초과율 전환 수입금액 임계표**(당해 수입 기준, 초과율 자동 적용에 필요)와 **단순경비율 배제(기준경비율 적용) 직전 수입금액 기준표**(가목 6,000만·나목 3,600만·다목 2,400만 — 작성사례에서 나목 3,600만 확인, 전 업종 확정표는 자료 수령 후 후보로). 0.16의 면세·기장의무는 법령이 SSOT이므로 별도 사용자 엑셀 없이 법령 MCP로 구현했다. ③그룹(무료 공공 API 키): juso.go.kr 주소검색, 공공데이터포털 사업자등록 상태조회(브라우저 CORS로 Edge Function 프록시 필요 가능). ②③은 자료·키 수령 후 진행하며 세무 항목은 후보(requires_review)+출처로 처리한다.

간편장부 import(#5) 파서 접근 실증: 하드룰 준수(무 CDN)로 ZIP 수작업 파싱 + `DecompressionStream('deflate-raw')` + 브라우저 `DOMParser`. 「장부」 시트 4행부터 A:L(A일자·C계정과목·D거래내용·E거래처·F/G수입·H/I비용·J/K자산·L비고). 0.15에서 파싱+미리보기(원본 행 보존, 확정 안 함) → 이후 확정→원장(중복 탐지)로 나눠 진행한다.

아직 구현하지 않은 기능을 완료된 기능처럼 보이게 하는 UI는 만들지 않는다.

## 변경 금지선

1. RLS를 제거하거나 `authenticated` 전체 허용 정책으로 바꾸지 않는다.
2. `service_role`, Cloudinary API secret, OAuth client secret을 브라우저·저장소·Git에 넣지 않는다.
3. `canonical_version`을 단순 양방향 upsert와 혼동하지 않는다. 최종본 변경을 감지한 기기는 클라우드 기준으로 수렴하고 재업로드하지 않는다.
4. 마이그레이션 계획 없이 Supabase·IndexedDB·백업의 데이터 계약을 바꾸지 않는다.
5. 법정서식 최신 검증 없이 세무 신고용 “확정” 출력이라고 표시하지 않는다.
6. 기존 비회계 Supabase 테이블(`news_items`, `language_sync_meta`, `vocab_items`, `usmle_cards`)을 이번 앱 작업 범위로 임의 변경하지 않는다.
7. 참고용 Excel·PDF·ZIP 파일을 명시적 요청 없이 커밋하지 않는다.
8. 원격 push나 배포는 사용자 요청 없이 실행하지 않는다.
9. 연결 가이드의 공개 설정값은 `APP_INFO`, `GuideService`, 런타임 진단 state에서 읽고 Client Secret은 표시하거나 저장하지 않는다.

## 배포 요청 처리

사용자가 “배포해주세요”라고 말하면 검증, 필요한 커밋, main 반영, 원격 push, 가능한 호스팅 배포, Claude 인수인계 메시지 작성을 포함해 진행한다.

현재 저장소는 `main` 직접 작업 흐름이다. 기능 브랜치에서 작업 중인 경우에는 사용자 승인 없이 강제 push, rebase, reset 같은 파괴적 재작성은 하지 않고, main 반영 전략을 먼저 확인한다.

## 필수 참조 문서

| 목적 | 문서 |
|---|---|
| 최상위 요구사항 | `docs/accounting-ledger-design-directive-v2.md` |
| 상세 아키텍처 | `docs/accounting-ledger-v1-detailed-design.md` |
| 데이터 계약 | `docs/accounting-ledger-v1-schema-design.md` |
| 화면/사용 흐름 | `docs/accounting-ledger-v1-screen-flow.md` |
| 연구노트 | `docs/accounting-ledger-app-research-notes.md` |
| 개발 품질 게이트 | `docs/skills/accounting-development-governance-skill.md` |
| 회계 도메인 Guardian | `docs/skills/accounting-domain-guardians-skill.md` |
| 코드 설계 Guardian | `docs/skills/accounting-code-architecture-guardians-skill.md` |
| 하네스 기준선 | `docs/accounting-ledger-harness-baseline.md` |
| 하네스 운영 스킬 | `docs/skills/accounting-harness-quality-gate-skill.md` |
| Claude 협업 규칙 | `docs/skills/accounting-claude-collaboration-skill.md` |
| 작업 요청 템플릿 | `docs/claude-task-template.md` |

## 작업 완료 인수인계 형식

작업을 마치면 아래를 짧게 남긴다.

| 항목 | 기록 내용 |
|---|---|
| 작업 | 구현 또는 조사한 내용 |
| 변경 파일 | 추가·수정·삭제 파일 |
| 데이터 영향 | Supabase, IndexedDB, import/export, 동기화 영향 여부 |
| 보안 영향 | Auth, RLS, 비밀키, 증빙 접근 영향 여부 |
| 검증 | 실행한 테스트와 결과 |
| 미완료/위험 | 남은 작업, 의도적으로 제외한 항목 |
| 버전/기록 | 앱·스킬 버전, 연구노트 갱신 여부, 커밋 |

중요 결정이나 다음 작업의 전제 조건이 바뀌면 이 문서와 `docs/accounting-ledger-app-research-notes.md`를 같은 작업에서 갱신한다.

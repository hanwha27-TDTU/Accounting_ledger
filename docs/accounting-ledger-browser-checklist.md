# Accounting Ledger 브라우저 수동 체크리스트

> 개정 2026-08-07 · 대상 앱 버전 `0.69`

이 문서는 자동화된 Required `browser-roundtrip`과 실계정·실기기에서만 확인 가능한 수동 시나리오를 함께 관리한다. 증빙 원본 암호화 왕복의 핵심 데이터 계약은 실제 Chromium으로 매 하네스 실행마다 검증하며, 화면 배치·운영 계정·실기기 항목은 아래 체크리스트 결과를 별도로 남긴다.

실행 방법: `index.html`을 브라우저로 연다(정적 파일이라 서버 불필요). 운영 확인은 `https://hanwha27-tdtu.github.io/Accounting_ledger/`에서 한다. 개발자 도구 콘솔을 열어 둔다.

## 공통 기준

- [ ] 콘솔 error 0건(경고는 사유 확인).
- [ ] 데스크톱(예: 1280px)과 모바일(예: 390x844) 모두 가로 overflow 없음.
- [ ] 긴 한국어 문구·URL이 잘리거나 겹치지 않음.

## 1. 로드와 오프라인 (0.01)

- [ ] 첫 로드시 대시보드가 뜨고, 사업자 미설정 안내가 보인다.
- [ ] 네트워크를 끊어도(오프라인) 거래 입력·저장이 되고, 상단 상태가 `오프라인 · 로컬 저장`으로 바뀐다.
- [ ] 다시 온라인이 되면 상태가 복구된다.

## 2. 사업자 설정과 거래·전표 (0.01)

- [ ] 설정에서 사업자 정보를 저장하면 상단 컨텍스트와 대시보드 안내가 갱신된다.
- [ ] 거래(수입/비용/자산) 입력 시 공급가액·부가세·합계 미리보기가 맞다.
- [ ] 저장 후 전표 검토에서 차변 합계 = 대변 합계(불균형이면 저장 차단).
- [ ] 장부 화면의 수입/비용/자산 열과 상태 배지가 맞다.
- [ ] (0.58) 거래·전표·증빙이 각각 101건 이상이면 100건 단위 이전/다음 페이지가 나타나고, 마지막 페이지까지 이동해도 누락·중복·가로 overflow가 없다.

## 2b. 국세청 업종코드 검색 선택 (0.11)

- [ ] 설정 → 국세청 업종코드에서 "경영 컨설팅" 검색 시 `741400 경영 컨설팅업` 등이 뜬다.
- [ ] "의원" 검색 시 `851201 일반의원` 등이 뜬다. 숫자 "741" 검색 시 해당 코드들이 뜬다.
- [ ] 결과를 클릭하면 코드(6자리)와 업종명이 자동으로 채워지고 "선택됨"에 표시된다.
- [ ] 후보 안내와 홈택스 공식 조회·경비율 고시 링크가 보이고 새 탭으로 열린다.
- [ ] 코드를 직접 입력하는 fallback도 여전히 가능하다. 저장 후 다시 열면 선택값이 유지된다.
- [ ] (0.13) "940600"이나 "자문" 검색으로 인적용역 코드를 고르면 "인적용역 · 사업자등록번호 없을 수 있음" 힌트가 뜬다.
- [ ] (0.13) 사업자등록번호를 비워두고도 사업자 정보가 저장된다(라벨에 `(선택)`, 안내 문구 표시).
- [ ] (0.14) 사업자등록번호에 틀린 숫자(예: 2208162518)를 넣으면 "검증 실패" 오류가 뜨고, 유효한 번호(2208162517)는 통과한다.
- [ ] (0.14) 로그인 상태에서 대표자명을 비우면 Google 이름이 자동으로 채워진다(수정 가능).
- [ ] (0.14) 업종코드를 검색·선택하면 큰 업종이 자동으로 설정된다(예: 741400 경영컨설팅 → 자문·전문직, 940xxx → 프리랜서).
- [ ] (0.15) 업종코드를 선택하면 경비율이 자동 표시된다(예: 741400 → 단순 77.3% · 기준 23.1%; 940903 → 초과율 46.4% 함께). "후보·귀속연도·출처" 표기 확인.
- [ ] (0.16) 업종코드를 선택하면 기장의무 기준이 자동 표시된다(예: 851201 의료 → 7,500만원, 501101 도소매 → 3억원, 151101 제조 → 1.5억원). "소득세법 시행령 §208⑤" 근거 표기 확인.
- [ ] (0.16) 부가세 면세 가능성이 근거 조문과 함께 후보로 표시된다(예: 851201 → 의료보건 용역 §26①5호 · 미용 등 일부 과세 · 개별 판단; 741400 컨설팅 → 면세 표시 없음). "개별 판단" 문구 확인.

## 2c. 해외 거래 입력 (0.28)

- [x] 거래 입력에서 `해외 거래` 체크 시 통화·외화 금액·환율 칸이 나타나고, 외화×환율이 총 금액에 자동으로 채워진다(공급가액·부가세 미리보기도 갱신). **헤드리스 검증: 100×1,350 → 135,000원.**
- [x] 과세유형이 `면세사업`인 사업자가 비용/자산으로 해외 거래를 입력하면 **대리납부 주의(§52)** 경고가 뜬다. **헤드리스 검증됨.**
- [x] (0.29) 해외 거래를 저장하면 통화·외화 금액·환율·해외 여부가 **구조화된 값으로 거래에 저장**되고, 장부 거래내용에 `🌐 해외` 배지가 붙는다(마우스오버로 통화·환율). **헤드리스 검증: is_overseas=true·USD·100·1350 저장·배지 표시.** 초기화 시 해외 칸이 접힌다.
- [x] (0.30) 통화 칸에서 목록(datalist)으로 통화를 고를 수 있다 — 기축·주요 통화(USD·EUR·JPY·GBP·CNY 등)와 **UZS 우즈베키스탄 숨** 포함(28개). 목록에 없는 통화도 3글자 코드로 직접 입력된다. **헤드리스 검증: 28개 옵션·UZS 존재.**

## 3. 연결 진단과 로그인 (0.02–0.03)

- [ ] 설정 → 연결 진단: Data API `정상`, 익명 회계자료 `차단 정상`, Google OAuth 상태 표시.
- [ ] (0.58) 로그인한 상태에서 연결 진단이 성공하면 별도 `지금 동기화` 클릭 없이 즉시 동기화되고 성공 안내가 `연결 확인 후 즉시 동기화했습니다.`로 보인다.
- [ ] 가이드 → 구글클라우드 연결방법: 값이 현재 설정(APP_INFO/GuideService)과 일치하고 복사가 된다.
- [ ] owner(`hanwha27@gmail.com`) 로그인 후 상단이 `동기화 완료`로 바뀐다. 비허용 계정은 차단된다.

## 4. businesses CRUD·RLS 왕복 (0.04)

- [ ] 로그인 상태에서 설정 → `사업자 CRUD·RLS 왕복 검증` 실행 시 생성·조회·수정·소프트삭제 격리·정리 5단계가 모두 `통과`.
- [ ] 검증 후 클라우드 `businesses`에 `__rls_probe__` 잔존 행이 없다.
- [ ] 비로그인 상태에서는 이 패널이 보이지 않는다.

## 5. 데이터 관리 (0.05, 0.39에서 카드 구성 갱신)

- [x] 관리 → 데이터 관리: 카드가 2열 대칭으로 보이고, 모바일에서 1열로 접힌다. **0.63 앱 내 브라우저 실측: 각 행 좌우 카드의 top·bottom·height가 동일하고 6개 카드 margin-top 0px, 콘솔 오류 0건. 모바일은 CSS breakpoint 계약 확인.**
- [ ] 상단 안내 문구가 "정본은 클라우드, 이 기기는 보조 캐시"라는 취지로 보인다.
- [ ] 이 기기 백업 → 파일 생성(파일명에 `-local-` 포함). 복원("이 기기만 복원") → 확인 후 로컬 데이터가 파일 기준으로 교체된다.
- [ ] 부분 삭제: 거래 1건 선택·삭제 시 목록에서 사라지고, 전표·라인도 함께 소프트삭제된다.
- [ ] 일괄 삭제: 이중 확인 후 로컬 장부가 비고, Supabase 연결 설정은 유지된다.

## 5f. 클라우드가 정본 — 백업·복원·강제 새로고침 (0.39)

owner로 로그인한 상태에서 확인(비로그인 시 관련 버튼은 비활성/숨김 처리만 확인):

- [ ] "클라우드에서 백업" 클릭 시 파일이 생성되고(파일명에 `-cloud-` 포함), 이 기기 캐시가 아니라 클라우드 값을 그대로 담고 있다(다른 기기에서 만든 거래도 포함되는지 확인).
- [ ] "클라우드 기준으로 새로고침" 클릭 시 확인창에 "아직 클라우드에 안 올라간 변경이 사라집니다" 경고가 대기 건수와 함께 뜨고(대기가 있을 때만), 실행 후 이 기기 화면이 클라우드 값과 정확히 같아진다.
- [ ] "이 기기만 복원"으로 오래된 백업을 복원한 뒤 잠시 기다리면(자동 동기화), 클라우드 값이 다시 우선해 화면이 원래대로 돌아올 수 있다 — 카드 설명 문구가 이 사실을 미리 안내하는지 확인.
- [ ] "복원 후 모든 기기에 반영"(owner 전용)으로 복원하면, 확인창에 "다른 모든 기기는 이 내용으로 맞춰지고 각 기기 로컬 전용 변경은 버려진다"는 경고가 뜨고, 성공 시 canonical 버전이 올라간다. 다른 기기(B)에서 다음 동기화 시 이 내용으로 실제로 맞춰지는지 확인.
- [ ] 클라우드에서 만든 백업 파일을 "이 기기만 복원"으로 복원해도 이 기기의 동기화 대기 건수(sync_queue)가 0으로 초기화되지 않는다(클라우드 백업엔 없는 로컬 전용 항목이라 보존돼야 함).

## 5b. 기기간 자동 동기화 — 기본값 (0.35)

기기 A, B 두 브라우저(또는 프로필)로 같은 owner 로그인:

- [ ] B에 로그인만 하고(동기화 버튼을 누르지 않음) 잠시 기다리면, A에서 만든 거래가 B에도 자동으로 나타난다.
- [ ] A에서 거래를 저장한 뒤 다른 탭으로 갔다가 다시 이 탭으로 돌아오면(포커스 복귀) 상단 동기화 배지가 자동으로 갱신된다(버튼을 누르지 않아도).
- [ ] 오프라인으로 전환 후 거래를 저장하고, 다시 온라인으로 전환하면 "자동으로 동기화합니다" 안내 후 자동으로 반영된다(버튼 클릭 불필요).
- [ ] 거래 입력 화면에서 입력 중(제출 전)에 백그라운드 자동 동기화가 발생해도 입력하던 값이 사라지지 않는다(다른 기기에서 동시에 거래를 저장해 동기화를 유발해 확인).
- [ ] 설정 화면의 폼(사업자 정보 등)에 입력 중일 때도 마찬가지로 값이 사라지지 않는다.
- [x] 마감 화면에서 마감 사유(`메모`)를 입력 중일 때도 값이 사라지지 않는다(0.40에서 `AUTO_SYNC_UNSAFE_ROUTES`에 `closing` 누락을 발견·수정 — 로그인 없이도 재현 가능한 `hashchange` 강제 재렌더 방식으로 **헤드리스 검증됨**).
- [ ] 데이터 관리 → 클라우드 동기화 카드 문구가 "자동으로 동기화됩니다"로 보이고, `지금 동기화` 버튼은 여전히 동작한다(즉시 반영용).
- [ ] 콘솔에 자동 동기화로 인한 에러가 없다(로그인하지 않은 상태에서 포커스·온라인 이벤트가 발생해도 조용히 무시된다).

## 5c. 가계부 여러 개 관리 (0.36)

- [ ] 설정에 "가계부 관리" 패널이 보이고, 현재 가계부에 "사용 중" 배지가 붙는다.
- [ ] "새 가계부 추가"로 이름·종류(개인/사업자)를 정해 만들면 즉시 그 가계부로 전환되고(topbar 이름이 바뀜), 거래·계정과목이 0건인 새 상태로 시작한다.
- [ ] 가계부 목록에서 "전환" 버튼을 누르면 다른 가계부로 바뀌고, 장부·리포트·대시보드가 그 가계부 자료만 보여준다(다른 가계부의 거래가 섞여 보이지 않는다).
- [ ] 개인 가계부와 사업자 가계부를 하나씩 만들어 오가도 서로의 모드(세무 화면 노출 여부)가 뒤섞이지 않는다.
- [ ] 클라우드 동기화 후 다른 기기에서도 같은 가계부 목록이 보인다(단, "지금 보고 있는" 가계부는 기기별로 다를 수 있다 — 정상 동작).
- [ ] 0.35 이전부터 쓰던 기존 가계부(가계부가 하나뿐이던 설치)는 업그레이드 후에도 그대로 활성 가계부로 유지되고, 모드(개인/사업자)도 그대로 보인다.
- [x] (0.49) 상단바(동기화 배지 앞)에 현재 가계부 이름이 적힌 전환 버튼이 있다. 가계부가 하나도 없는 첫 실행 화면에서는 버튼이 안 보이다가, 첫 가계부를 만들면 그 이름과 함께 나타난다. **헤드리스 검증됨.**
- [x] (0.49) 전환 버튼을 누르면 "가계부 전환" 모달이 뜨고 모든 가계부가 나열된다(활성 가계부는 "사용 중" 배지, 나머지는 "전환" 버튼). 다른 가계부의 "전환"을 누르면 모달이 닫히고 상단바 버튼 라벨이 즉시 그 가계부 이름으로 바뀐다(장부·리포트도 그 가계부 자료만 보임). **헤드리스 검증됨.**
- [x] (0.49) 모바일 폭(390px 등)에서는 버튼 텍스트가 숨고 아이콘만 남지만 버튼 자체는 계속 보이며, 가로 스크롤(overflow)이 생기지 않는다. **헤드리스 검증됨.**

## 5d. 마감·마감해제 (0.37)

- [ ] 개인 가계부 모드에서도 왼쪽 메뉴에 "마감"이 보인다(0.37부터 사업자 전용이 아님).
- [ ] 이번 달을 마감하면 목록에 "마감됨" 배지가 뜨고, 몇 건이 있는지 표시된다.
- [ ] 마감한 달 안의 날짜로 새 거래를 저장하려 하면 거래일 칸에 "이 날짜는 마감된 기간입니다..." 오류가 뜨고 저장되지 않는다.
- [ ] 마감하기 전에 이미 있던 거래를(데이터 관리 → 선택 거래 삭제) 지우려 하면 "마감된 기간이라 삭제할 수 없습니다" 안내가 뜨고 실제로 지워지지 않는다.
- [ ] "마감 해제"를 누르면(확인창) 배지가 "열림"으로 바뀌고, 그 즉시 같은 달에 거래를 새로 저장하거나 지울 수 있게 된다.
- [ ] 마감·해제 기록에 날짜(마감일, 마지막 해제일)가 남아 있다 — 마감 이력이 사라지지 않는다.
- [ ] 다른 가계부로 전환하면 마감 목록도 그 가계부 것만 보인다(가계부별로 분리).

## 5e. 가계부 삭제 (0.38)

- [ ] 가계부 관리 목록의 각 가계부에 "삭제" 버튼이 보인다(사용 중인 가계부도 포함).
- [ ] 삭제를 누르면 확인창에 가계부 이름과 "거래·계정과목·거래처·전표·증빙·마감 기록을 삭제합니다"라는 문구가 뜬다.
- [ ] 사용 중이 아닌 가계부를 삭제하면 목록에서 사라지고, 다른 가계부·거래에는 영향이 없다.
- [ ] **지금 쓰던(활성) 가계부**를 삭제하면 남은 가계부 중 하나로 자동 전환되고(topbar 이름이 바뀜), 그 가계부의 장부·거래는 그대로 보인다.
- [ ] **마지막 남은 가계부**를 삭제하면 확인창에 "마지막 남은 가계부입니다" 경고가 추가로 뜨고, 삭제 후 "가계부 관리"엔 목록이 비고 "가계부 기본정보"는 빈 새 가계부 입력 폼으로 돌아간다(앱이 깨지지 않는다).
- [ ] 삭제한 가계부의 거래를 다른 가계부 화면에서 찾을 수 없다(완전히 격리돼 사라짐).
- [ ] 콘솔에 에러가 없다.

## 5f. 재산세 계산기(주택분) (0.42)

- [x] 왼쪽 메뉴 맨 아래("계산기" 그룹, 가이드 바로 아래)에 "재산세 계산기"가 보인다. **헤드리스 검증됨.**
- [x] 공시가격(시가표준액)을 입력하면 즉시(버튼 없이) 재산세·도시지역분·지방교육세·합계가 계산된다. **헤드리스 검증: 3억원 입력 시 값 갱신 확인.**
- [x] "1세대 1주택입니다" 체크 시 공정시장가액비율이 43~45%로 바뀌고, 시가표준액 9억원 이하면 특례세율표가 적용된다(3억원 1세대1주택 → 공정시장가액비율 43%, 합계 299,400원). 체크 해제 시 즉시 60%·표준세율표로 되돌아간다. **헤드리스 검증됨.**
- [x] "도시지역분을 포함합니다" 체크 해제 시 도시지역분이 0원이 된다. **헤드리스 검증됨.**
- [x] 직전연도 공시가격을 입력하고 과세표준상한액을 초과하면 "(과세표준상한액 적용)" 문구가 뜬다. 비워두면 상한 계산 자체를 생략한다. **헤드리스 검증됨.**(0.46에서 입력값이 "직전연도 과세표준" 직접입력에서 "직전연도 공시가격" 입력으로 바뀜 — 아래 0.46 항목 참고)
- [ ] 계산 결과가 화면에서 잘리거나 겹치지 않는다(모바일 폭 포함).
- [ ] "지금은 주택만 계산합니다" 안내와 지역자원시설세(소방분)·토지·건축물·선박·항공기 미구현 안내가 명확히 보인다.
- [ ] 콘솔에 에러가 없다(파비콘 404 제외 — 앱에 favicon 링크가 없어 브라우저가 자동 요청하는 것으로 앱 로직과 무관).
- [x] (0.43) "공유 지분율(%)" 칸이 있고 기본값이 100이다. **헤드리스 검증됨.**
- [x] (0.43) 지분율을 100 미만(예: 50)으로 바꾸면 "물건 전체 재산세 합계"와 "공유 지분율" 행이 새로 나타나고, 재산세·도시지역분·지방교육세·합계 라벨에 "· 내 지분 몫"이 붙는다. 100으로 되돌리면 두 요소 모두 사라져 단독소유 때와 화면이 완전히 같아진다. **헤드리스 검증됨.**
- [x] (0.43) 실제 재산세 고지서(서초구, 2026년 7월, 동일지분 2인 공동명의 1세대1주택)의 실제 시가표준액(공시가격 알리미 기준 1,578,000,000원)·직전연도 과세표준(575,100,000원, 2025 공시가격×45%)·1세대1주택 체크·지분율 50%를 넣으면, 과세표준상한액이 적용되어 과세표준이 고지서와 정확히 같은 610,605,000원이 되고, 재산세 본세가 고지서 실측값(연간 906,200원)과 10원 이내로 일치한다. **헤드리스 검증됨(실제 고지서 대조).**
- [x] (0.44) 공시가격 입력칸 아래에 "같은 해 1월 1일 기준 공동주택가격을 넣으라"는 안내(4월 30일 확정공시, realtyprice.kr)가 보인다. **헤드리스 검증됨.**
- [x] (0.44) 결과 요약 아래에 "계산 과정"이 번호 붙은 문장으로 나온다(공시가격×비율 → 상한 적용 여부 → 세율 적용 → 도시지역분 → 지방교육세 → 지분율 → 합계). 위 실제 고지서 시나리오를 넣으면 계산 과정 문장에 "과세표준상한액"과 정확한 캡핑값(610,605,000원)이 등장하고, 지분율 50%도 등장하며, 마지막 줄의 합계 숫자가 위 요약 패널의 합계와 정확히 같다. **헤드리스 검증됨.**
- [x] (0.45) "직전연도 과세표준" 입력칸 아래에 "작년 고지서의 재산세과표 항목을 넣거나 위택스에서 조회하라"는 안내와 "비율은 해마다 달라질 수 있어 직접 계산하면 틀릴 수 있다"는 경고가 보인다. **헤드리스 검증됨.**(0.46에서 이 입력칸 자체가 아래처럼 정정됨)
- [x] (0.46) 입력칸이 "직전연도 과세표준"에서 "직전연도 공시가격"(`#ptPriorMarketValue`)으로 바뀌었고, field-help가 §109의2① 메커니즘(직전연도 공시가격에 항상 올해 공정시장가액비율을 곱해 유도 — 작년 실제 비율이 아님)을 설명하며 "작년 고지서의 과세표준 금액을 그대로 넣지 말라"고 경고한다. 실제 고지서 시나리오(공시가격 1,578,000,000원, 직전연도 공시가격 1,278,000,000원, 1세대1주택, 지분 50%)를 넣으면 계산 과정 문장에 유도된 "작년 과세표준 상당액" 575,100,000원이 나타나고, 과세표준상한액이 적용되어 과세표준 610,605,000원(기존과 동일)이 나온다. **헤드리스 검증됨.**
- [x] (0.47) 예상 납부세액 합계가 20만원을 넘으면 그 아래에 "7월 16~31일 납부분(1기분)"·"9월 16~30일 납부분(2기분)"이 절반씩(합이 합계와 정확히 일치) 표시된다. 3억원 1세대1주택 시나리오(합계 299,400원)에서 두 칸(`#ptFirstInstallment`/`#ptSecondInstallment`)에 각각 149,700원이 뜨고, 계산 과정 마지막 줄에도 "1기분"·"2기분" 금액이 문장으로 등장한다. **헤드리스 검증됨.**
- [x] (0.47) 합계가 20만원 이하면(예: 1억원 일반주택, 합계 156,000원) 위 두 분납 행 대신 "20만원 이하 · 7월에 한꺼번에 낼 수도 있음(지자체 조례 따라 다름)" 안내 한 줄이 뜨고, 계산 과정에도 같은 취지의 문장이 등장한다. **헤드리스 검증됨.**
- [x] (0.48) 공시가격·직전연도 공시가격·건물 시가표준액 입력칸 아래에 입력값을 억/만원 단위로 읽어주는 표기(예: 1,578,000,000 입력 시 "15억 7,800만원")가 실시간으로 뜬다. **헤드리스 검증됨.**
- [x] (0.48) "건물 시가표준액(지역자원시설세용)" 입력칸이 있고 기본은 비어 있다. 비워두면 지역자원시설세(소방분) 결과 블록이 아예 안 보이고, 값을 넣으면 소방분 세액과 납부 시기(20만원 초과면 1기분·2기분, 이하면 일괄징수 안내)가 재산세 합계와 별도 블록으로 나타나며 계산 과정에도 "지역자원시설세" 설명이 추가된다. **헤드리스 검증됨.**
- [x] (0.48) "계산 근거" 섹션 아래에 공정시장가액비율·재산세 과세표준·재산세 과세표준상한액·재산세 세율·재산세 도시지역분·지방교육세·지역자원시설세(소방분)·반기분납 용어 카드가 쉬운 설명과 법적 근거(지방세법 조문)와 함께 나열된다. **헤드리스 검증됨.**

## 6. 다기기 삭제 수렴 — 좀비 방지 (0.06)

기기 A, B 두 브라우저(또는 프로필)로 같은 owner 로그인:

- [ ] A에서 거래를 만들고 동기화 → B에서 동기화 시 그 거래가 보인다.
- [ ] A에서 그 거래를 삭제하고 동기화 → B에서 동기화 시 **B에서도 사라진다**.
- [ ] B에서 다시 동기화해도 삭제된 거래가 되살아나지 않는다(멱등).
- [ ] 삭제한 A에서도 재동기화 시 그 거래가 되돌아오지 않는다.

### 최종본 지정 (0.08, owner 전용)

- [ ] owner로 로그인하면 데이터 관리 동기화 카드에 `이 기기를 최종본으로` 버튼이 보인다(비-owner에겐 없음).
- [ ] B에 아직 동기화하지 않은 로컬 거래가 있는 상태에서 A가 `이 기기를 최종본으로`를 실행한다(확인창 뜸).
- [ ] A 실행 후 canonical 표시가 올라간다.
- [ ] B에서 동기화하면 B의 로컬 전용 미동기화 거래가 사라지고 A(클라우드) 기준으로 맞춰지며 canonical이 같아진다.
- [ ] (0.58) 클라우드에 A·B 두 가계부가 있고 최종본 기기에는 A만 있는 상태에서 최종본 지정 → 다른 기기 동기화 후 B와 B의 거래·전표·증빙이 다시 살아나지 않고 A만 남는다(audit_logs는 보존).
- [ ] (0.58) 삭제 이력이 있는 같은 ID를 백업 복원 후 최종본으로 지정하면, 과거 tombstone보다 새 `updated_at`을 가진 복원 행은 다음 일반 동기화에서도 다시 삭제되지 않는다.

### 빈 클라우드 가드 (0.10)

- [ ] 로컬에 사업자·거래가 있는 상태에서, 클라우드가 회계자료를 못 주는 상황(예: 잘못된 로그인/권한)에 최종본 적용이 실행돼도 **로컬 데이터가 지워지지 않고** "로컬 데이터를 지키려고 최종본 적용을 중단했습니다" 안내가 뜬다.

## 7. 허용 사용자 관리 — owner 전용 (0.07)

owner(`hanwha27@gmail.com`)로 로그인:

- [ ] 설정에 `허용 사용자 관리(owner 전용)` 패널이 보이고, 현재 허용 목록이 표시된다.
- [ ] 이메일·역할·메모로 사용자를 추가하면 목록에 `활성`으로 나타난다.
- [ ] `차단`을 누르면 상태가 `차단`으로 바뀌고, `허용`으로 되돌릴 수 있다.
- [ ] 소유자 자기 행은 `보호됨`으로 차단 버튼이 없다.
- [ ] 잘못된 이메일·중복 이메일 추가 시 명확한 오류 메시지가 뜬다.
- [ ] owner가 아닌 계정으로 로그인하면 이 패널이 보이지 않는다.
- [ ] 차단(blocked)된 계정으로 로그인하면 회계자료에 접근하지 못하고 차단된다.

## 8. Cloudinary 증빙 첨부 (0.09) — 사용자 Cloudinary 설정 필요

사전: Cloudinary 계정에서 제한된 **unsigned upload preset**(허용 형식 jpg·png·pdf, 폴더·최대 크기 제한) 생성.

- [ ] 증빙 화면: Cloudinary 미설정 시 설정 안내 배너와 `설정 열기`가 보인다.
- [ ] 설정 → Cloudinary 증빙 저장소에 cloud name과 unsigned preset을 저장하면 배지가 `설정됨`으로 바뀐다.
- [ ] 비밀키 형태(`cloudinary://`로 시작하는 API 환경변수 URL이나 secret 값)를 넣으면 저장이 거부된다.
- [ ] 거래의 `파일 첨부`로 이미지 업로드 시 썸네일이 뜨고 배지가 `1개 첨부`로 바뀐다.
- [ ] PDF 업로드 시 파일 링크가 뜨고 클릭하면 원본이 열린다.
- [ ] 오프라인에서 첨부해도 로컬에 저장되고, 로그인·동기화 후 클라우드(evidence_files)에 반영된다.
- [ ] 15MB 초과 파일은 첨부가 거부된다.
- [ ] 잘못된 preset이면 명확한 오류 메시지가 뜬다.
- [ ] (0.12) 첨부된 증빙의 × 버튼으로 제거하면 목록에서 사라지고, 그 거래에 다른 증빙이 없으면 `미첨부`로 돌아간다. 두 기기면 동기화 후 다른 기기에서도 사라진다.

## 8b. 간편장부 계정과목 분류표 (0.17)

- [ ] 가져오기 화면에 `간편장부 계정과목 분류표` 패널이 보이고, 수입·비용·제조비용·자산 4개 그룹으로 25개 계정과목과 공식 설명이 나온다.
- [ ] 각 그룹 머리말에 `간편장부 수입금액/비용/자산 열` 대응이 표시된다(수입→수입금액, 비용·제조비용→비용, 자산 매입→자산).
- [ ] 데스크톱·모바일 모두 표가 가로 overflow 없이 접힌다(설명이 길어도 줄바꿈).

## 8c. 추계소득 계산기 (0.18 엔진 · 0.19 UI)

- [ ] 리포트 화면에 `추계소득금액 계산 방법(기준경비율)` 카드가 `검토용` 배지와 함께 보이고, 소득금액 = min(①,②) 공식과 복식부기 ½·3.4배 / 간편장부 2.8배가 표시된다.
- [ ] `복식부기의무자` 추계 시 가산세(무신고 vs 장부 기록·보관 불성실 중 큰 금액) 경고가 보인다.
- [x] (0.19) 계산기: 총수입·매입·임차·인건비를 넣고 기장의무를 고르면 주요경비·①·②·소득금액·필요경비가 실시간 갱신된다. **헤드리스 브라우저로 작성사례 수치 검증됨**(120,000,000·주요경비 92,960,000·복식부기 → ① 21,220,000·② 42,024,000·소득 21,220,000·필요경비 98,780,000; 간편장부 → ② 34,608,000·소득 15,400,000).
- [ ] (0.19) 업종코드가 설정된 사업자는 기준·단순경비율이 자동으로 채워지고(수정 가능), 미설정 시 직접 입력해도 계산된다. 데스크톱·모바일 모두 입력 그리드가 넘치지 않는다.
- [ ] (0.20) 계산기에 `직전연도 수입금액`을 넣으면 `기장의무 판정: 복식부기의무자/간편장부대상자`와 `경비율 적용: 단순/기준경비율` 태그가 뜨고, 기장의무 select가 자동으로 맞춰진다(업종코드 설정 필요). 서비스 로직은 헤드리스로 검증됨(851201: 3천만↑→기준·복식, 2천만→단순·간편).

## 8d. 가이드 법령 안내 버튼 (0.20)

- [ ] 가이드 화면에 버튼 4개(구글클라우드 연결 + 기장의무 판단 + 부가가치세 면세 + 추계신고와 경비율)가 보인다.
- [ ] 각 법령 버튼을 누르면 쉬운 설명 + 기준금액 + `근거` 블록(조문)이 나온다. **헤드리스로 근거 조문 렌더 검증됨**(기장의무=소득세법 §160·시행령 §208⑤; 추계=소득세법 §80③·시행령 §143; 면세=부가가치세법 §26①).
- [ ] 기장의무·추계 버튼의 `설정에서 업종코드 고르기`/`리포트에서 계산기 열기` 링크가 해당 화면으로 이동한다.
- [x] (0.21) 각 법령 버튼에 `왜 이렇게 정했을까요? (눈높이 설명)` 블록이 목적·취지와 함께 보인다. **헤드리스로 3개 토픽 모두 렌더 검증됨**(기장의무·면세·추계).
- [x] (0.22) 가이드에 `주요 세법 용어사전` 버튼이 있고, 28개 용어가 5개 분류(장부·기장/소득·세액/경비율·추계/부가가치세/증빙·경비)로 법적 정의(근거 조문)와 초딩 설명과 함께 표시된다. **헤드리스로 28개 카드·5개 그룹 렌더 검증됨.**
- [x] (0.22) 대시보드 `세법 용어 검색`에서 단어(예: 필요경비, 경비율)를 치면 관련 용어 카드가 뜨고, 없는 단어는 안내가 뜬다. `전체 용어 보기`가 가이드 용어사전으로 이동한다. **헤드리스로 검색·이동 검증됨.**
- [x] (0.27) 가이드에 `해외 구매·수입 처리` 버튼(총 6개 토픽)이 있고, 해외 물품 수입·해외 용역(대리납부)·현지 소비 구분과 면세사업자 대리납부 경고가 근거(§4·§52)와 함께 뜬다. 용어 검색에서 대리납부·수입부가세·기준환율이 나온다. **헤드리스로 렌더·경고·검색 검증됨.**
- [ ] (0.23) 거래 입력 화면의 용어(계정과목·공급가액 등) 밑줄 버튼을 누르면 툴팁이 뜬다. 세법 용어(복식부기·공급가액)는 용어사전과 같은 설명, 앱 용어(부가세·원천거래·전표)는 기존 설명. **용어 해석 경로(용어사전 우선→앱용어 폴백)는 헤드리스로 검증됨**(툴팁 DOM 클릭은 사업자 설정 시 노출되어 수동 확인 대상).

## 8e. 간편장부 Excel 미리보기 (0.24)

- [x] 가져오기 화면에서 국세청 간편장부(.xlsx)를 선택하면 「장부」 시트를 읽어 **읽은 건수·귀속연도·수입/비용/자산 건수·합계**가 뜬다. **실제 사용자 샘플(945건, 2025년, 수입 40,000,000·비용 38,625,037)로 헤드리스 브라우저 검증 완료.**
- [x] 미리보기 표가 일자(월·일 합쳐 YYYY-MM-DD)·계정과목·거래내용·거래처·수입/비용/자산·비고를 원본대로 보여준다(상위 200건). 분류표에 없는 계정과목은 `(미분류)` 표시.
- [ ] 잘못된 파일(장부 시트 없음)이면 명확한 오류가 뜬다.
- [ ] 데스크톱·모바일 모두 미리보기 표가 가로 스크롤 컨테이너 안에서만 넘친다(본문 overflow 없음).
- [x] (0.25) `장부에 반영` 버튼을 누르면 확인 후 거래가 복식부기 원장에 기록되고 장부 화면으로 이동한다. **실 샘플로 헤드리스 검증: 945건 전기·차변=대변 81,651,988(전부 균형)·계정과목 자동생성 6개.**
- [x] (0.25) 같은 파일을 다시 반영해도 **중복이 쌓이지 않는다**(내용 기반 결정적 id). 헤드리스 재반영 검증: 거래 수 945 유지, 균형 유지.
- [ ] (0.25) 사업자 미설정 시 반영 버튼이 비활성이고 설정 안내가 뜬다. 없던 계정과목·거래처는 자동 생성된다(장부에 이름 그대로 표시).
- [x] (0.26) 업로드하면 **검증 모달이 자동으로 열려** 정상/경고/오류 건수를 보여준다. **실 샘플 헤드리스 검증: 정상 945·경고 512(부가세)·오류 0.** 미분류 계정과목은 드롭다운으로 표준 계정과목 치환, 부가세 총액 의심은 `일괄 적용`으로 공급가액 재계산 — 값 변경은 승인해야 반영.
- [x] (0.26) `일괄 적용` 후 경고 512→0, 반영 시 945건 균형 유지(차변=대변). 오류 행은 반영에서 자동 제외. **헤드리스 검증 완료.**
- [ ] (0.26) 모달의 닫기/X로 닫히고, 인라인 `검증·반영 창 열기` 버튼으로 다시 열린다. 데스크톱·모바일 모두 모달이 화면 안에 들어온다.

## 8f. 월별 사용현황 (0.31)

- [x] 상단(모든 화면 공통)에 `월별 사용현황` 버튼이 있다. 사업자 미설정 시 클릭하면 오류 안내만 뜨고 모달은 열리지 않는다. **헤드리스 검증됨.**
- [x] 사업자·거래가 있으면 버튼 클릭 시 모달이 열리고, 계정과목(열) × 월(행) 표에 금액이 정확히 합산되어 보이며 행·열 합계가 맞는다. **헤드리스 검증: 1월 1,500원·2월 7,000원·합계 8,500원 정확.**
- [ ] 기간 선택(전체/특정 연도)을 바꾸면 표가 갱신된다. 계정과목이 많아도 가로 스크롤로 보이고(열 머리글·첫 열 고정), 모바일에서도 본문 overflow 없이 스크롤된다.

## 8g. 계정과목 선택 가이드 (0.32)

- [x] 가이드에 `계정과목 선택 가이드` 버튼(총 7개 토픽)이 있고, 25개 계정과목이 법적 정의·초등학생 설명·예시와 함께 그룹별로 보인다. `소모품비 vs 비품` 등 헷갈리는 것 구분법이 뜬다. **헤드리스 검증됨(25개 카드, 구분법 문구 확인).**
- [x] 거래 입력 화면 `계정과목` 라벨 옆 `선택 가이드` 버튼을 누르면 가이드의 계정과목 선택 가이드로 바로 이동한다. **헤드리스 검증됨.**
- [ ] 데스크톱·모바일 모두 카드 목록이 overflow 없이 보인다.

## 8h. 개인 가계부 모드 (0.33)

- [x] 사업자 정보가 없는 첫 실행 시 `개인 가계부로 시작` / `사업자로 시작` 두 카드가 뜨고, 다른 화면으로는 접근되지 않는다. **헤드리스 검증됨.**
- [x] `개인 가계부로 시작` 선택 → 설정 화면으로 이동, 사업자 주체·사업자등록번호·업종코드·과세유형·사업장 주소 칸이 숨고 `가계부 이름`으로 저장하면 계정과목 24개(배관용 6 + 개인 카테고리 18)가 만들어진다. **헤드리스 검증됨(24개 계정, 이름 목록 확인).**
- [x] 개인 모드 거래 입력: 비용 유형에 식비·교통비 등 12개 개인 카테고리만 뜨고(사업용 소모품비 등 없음), 부가세 처리 칸은 숨는다. `식비 30,000원` 저장 시 차변=대변(균형 전표)으로 정상 기록된다. **헤드리스 검증됨(균형 확인).**
- [x] 개인 모드 자산구입 유형: 예금/적금·가전/가구·차량이 선택되고 현금·보통예금 같은 배관용 계정은 뜨지 않는다. **헤드리스 검증됨.**
- [x] 개인 모드 네비게이션에는 전표 검토·가져오기·마감이 없다. 가이드에는 기장의무·부가세 면세·추계신고와 경비율·해외 구매 토픽이 없고 구글클라우드·계정과목 선택 가이드·용어사전만 남는다. **헤드리스 검증됨.**
- [x] 설정 → `계정과목 관리`에서 새 계정과목(예: 반려동물, 비용)을 추가하면 거래 입력 드롭다운에 바로 뜨고, `비활성화`하면 관리 목록과 드롭다운 양쪽에서 사라진다(과거 그 계정으로 쓴 거래의 이름은 장부에 그대로 남아야 함 — 실사용 확인 필요). **추가·선택·비활성화 흐름은 헤드리스 검증됨.**
- [x] 설정의 `개인 가계부 / 사업자` 전환 버튼으로 모드를 바꾸면 데이터 손실 없이 네비게이션·필드 노출이 즉시 바뀐다. **헤드리스 검증됨(전환 후 12개 메뉴·필드 복원 확인).**
- [x] **회귀 확인**: `사업자로 시작` 선택 시 기존 동작과 완전히 동일 — 계정 15개 시딩, 비용 드롭다운 6종(소모품비 등), 자산 드롭다운 비품만, 과세 거래(11,000원 과세) 저장 시 균형, 리포트에 추계소득 계산·법정서식 섹션 표시. **헤드리스로 전부 재확인, 차이 없음.**
- [ ] 데스크톱·모바일 모두 모드 선택 카드·계정과목 관리 폼이 overflow 없이 보인다.

## 9. 개발 기록·가이드·접근성

- [ ] 개발 기록: 앱 버전, 저장소·동기화 상태(CRUD 왕복 포함), Guardian 레지스트리가 보인다.
- [ ] 키보드 이동, 버튼 라벨, 터치 크기, 색 대비가 무리 없다.

## 10. 안드로이드 앱 설치·셸 (0.50) — 아래 항목은 실제 안드로이드 기기 필요, 브라우저로 확인 불가

가이드의 "안드로이드 앱 설치" 화면(브라우저에서도 열림)은 헤드리스로 검증됐다(다운로드 링크·안내 문구·`checkForWebUpdate()` 안전 동작). 아래는 실제 안드로이드 기기 또는 BlueStacks에서만 확인할 수 있는 항목이다. Android 화면·모바일 레이아웃을 변경하거나 배포할 때는 설치된 BlueStacks를 우선 사용하고, 인스턴스명·해상도·DPI·방향을 기록한다. 모바일 합격 판정은 세로 휴대폰 방향에서 수행하며, 가로 데스크톱 분기 확인은 보조 결과로만 남긴다.

- [ ] (BlueStacks, 배포 때마다) 공개 GitHub Pages 주소를 Android Chrome에서 열고 배포 버전·상단 safe-area·상단바·가로 overflow·드로어/scrim 터치·모달 스크롤을 확인한다. 입력 화면 변경 시 소프트 키보드가 활성 필드를 가리지 않는지도 확인한다. 실행할 수 없으면 `MANUAL/미검증`으로 기록한다. **앱 0.65 배포에서는 Pie64 1920×1080·240 DPI 가로 Android Chrome으로 공개 v0.65 정상 렌더까지 확인. 세로 모바일·키보드 검증은 미완료이므로 체크는 열어 둔다.**

- [x] 가이드 → "안드로이드 앱 설치"에서 다운로드 버튼을 누르면 `APK_INFO.downloadUrl`(고정 태그 `apk-latest`)로 이동한다. **헤드리스 검증됨(링크 값 일치만 — 실제 GitHub 릴리스 존재 여부는 CI가 최소 1회 성공 실행된 뒤에만 확인 가능).**
- [x] (실기기) 안드로이드 폰의 크롬에서 위 링크로 APK를 내려받아 설치했다 — 사용자가 실제로 앱을 설치해 0.51~0.53에 걸쳐 계속 사용 중임을 스크린샷으로 확인(2026-08-01). "출처를 알 수 없는 앱" 경고 화면 자체의 스크린샷은 못 받았지만 설치가 완료된 결과는 반복 확인됨.
- [x] (실기기) 설치 후 앱을 열면 배포된 웹과 화면이 완전히 동일함을 확인(2026-08-01) — 사이드바·대시보드·설정·가이드 화면 스크린샷이 웹 구조와 일치.
- [x] (실기기) 설정에서 "Google로 로그인"을 누르면 시스템 브라우저(Chrome)가 `accounts.google.com`을 열고, 계정 선택 후 앱으로 자동 복귀해 "로그인되었습니다" 토스트 + "동기화 중"→"동기화 완료" 상태로 바뀜을 스크린샷으로 확인(2026-08-01, 0.53 배포 직후). Supabase Redirect URLs 등록도 사용자가 스크린샷으로 확인시켜줌. **0.50~0.52는 이 과정에서 실패했었다** — 원인은 `flowType:'pkce'` 미지정(0.53에서 수정, 연구노트 참고).
- [x] (실기기) 웹만 바뀐 배포(0.51→0.52→0.53) 후 앱을 완전히 껐다 다시 여는 것만으로 재설치 없이 매번 최신 화면으로 바뀜을 3회 연속 확인(2026-08-01). 입력 중 갱신 지연 동작 자체는 실기기에서 별도로 재현·확인하지 않음(로직은 헤드리스로만 검증됨).
- [ ] (실기기, `android-shell/**`가 바뀌어 새 셸 빌드가 나온 뒤) 앱을 열면 새 버전이 있다는 토스트가 뜨고 다운로드가 자동으로 시작된다. 다운로드 완료 알림을 누르면 안드로이드 표준 설치 확인 화면이 뜨고, "설치"를 누르면 갱신이 끝난다(이 한 번의 확인은 안드로이드 보안 정책상 앱이 대신 눌러줄 수 없다 — 정상 동작). 같은 버전에 대해 반복해서 안내가 뜨지 않는다. **아직 미확인** — 0.50 이후 `android-shell/**` 자체가 바뀐 적이 없어(0.51~0.53은 모두 웹 전용 변경) 이 플로우가 실제로 트리거된 적이 없다.
- [x] (CI, GitHub Actions) `android-shell/**` 변경을 main에 반영하면 `.github/workflows/android-apk.yml`이 성공적으로 실행되어 `apk-latest` 릴리스에 `bareunjangbu-latest.apk`·`bareunjangbu-shell-version.json`이 갱신됨을 GitHub API로 확인(2026-08-01, run 30711141964, `signed:true`로 release 서명 — GitHub Secrets 4종 정상 등록 확인됨).

## 11. 고정지출현황 (0.59)

- [x] 데스크톱 상단바의 가계부 전환 버튼 왼쪽에 `고정지출현황` 버튼이 보이고, 월 환산액·활성 건수가 한 줄에 잘리지 않고 표시된다. **앱 내 브라우저 1264×710 검증됨.**
- [x] 모바일 폭에서는 같은 버튼이 달력 아이콘만 남아 상단바를 넘치지 않으며 접근성 이름/툴팁으로 기능을 알 수 있다. **앱 내 브라우저 390×844 검증됨.**
- [x] 새 항목에 서비스명·비용 계정과목·금액·주기·다음 납부일을 넣어 저장하면 목록과 상단 요약이 즉시 갱신된다. 전체 카드·계좌번호를 넣을 수 없고 끝 4자리만 허용된다. **29,000원 월간 항목 등록·31,000원 수정 검증됨.**
- [ ] 매월 31일 항목을 2월에 거래 입력하면 다음 납부일이 2월 말일로 보정되고, 이어서 입력하면 3월 31일로 복귀한다.
- [x] `거래 입력`을 누르면 비용 거래 폼에 서비스명·계정과목·예상금액·거래일이 미리 채워지고, 저장 시 균형 전표 생성과 다음 납부일 이동이 함께 완료된다. **29,000원 거래·균형 전표 저장과 2026-08-10 → 2026-09-10 이동 검증됨.**
- [x] 일시중지 항목은 월 환산액·30일 예정액에서 빠지고 거래 입력 버튼이 숨으며, 다시 사용하면 즉시 복원된다. **앱 내 브라우저 검증됨.**
- [ ] 해지·완전삭제가 각각 상태/소프트삭제 규칙대로 동작한다.
- [ ] 다른 가계부로 전환하면 고정지출 목록도 해당 가계부 자료로 즉시 바뀐다.
- [x] Supabase migration 0.05는 운영 DB에 적용했고 RLS·권한·스키마를 검증했다.
- [ ] 두 기기에서 등록·수정·해지·삭제·최종본 지정을 왕복해 `fixed_expenses` 개수와 다음 납부일이 일치하는지는 실기기에서 확인한다.

## 12. 다중통화·일일환율 (0.60)

- [x] 거래 입력과 고정지출 폼의 통화 선택에는 원(KRW)·숨(UZS)·달러(USD)·엔(JPY)만 보이며 기본값은 원이다. **앱 내 브라우저 및 로직 테스트 검증됨.**
- [x] 외화를 선택하면 CBU 공식환율·실제 고시일·원화 환산액이 표시되고, 거래일을 바꾸면 해당 날짜 기준으로 갱신된다. 주말 날짜는 직전 고시일이 표시된다. **USD 100 및 JPY 1,000 환산·CBU 고시일 표시 검증됨.**
- [ ] USD 100을 저장하면 원금액·통화·환율·환율일·출처가 원천거래에 남고, `total_amount`와 차변·대변은 같은 원화 확정액으로 균형을 이룬다.
- [x] 통화를 외화로 골라도 해외 거래 체크가 자동으로 켜지지 않고, 원화 상태에서도 해외 거래 체크를 별도로 선택할 수 있다.
- [x] 환율을 직접 수정하면 수동 환율 안내가 나오고 `exchange_rate_manual=true`, `exchange_rate_source=MANUAL`로 저장된다. **USD 환율을 1,500원으로 수정해 150,000원 환산 및 MANUAL 계약을 확인함.**
- [ ] 네트워크 실패 + 캐시 있음이면 마지막 정상 환율 경고가 보이고 거래는 검토 필요가 된다. 캐시도 없으면 환율이 비워져 저장이 차단된다.
- [x] 외화 고정지출 목록은 외화 원금액과 사용자가 고른 기준일의 예상 원화액을 함께 보여주고, 목록·상단·모달 요약이 같은 저장 환율을 사용하며 `amount`·`updated_at`을 조용히 덮어쓰지 않는다. **순수 함수 및 렌더링 경로 검증됨.**
- [ ] 외화 고정지출에서 실제 거래를 만들면 거래일 환율을 다시 확정하고 다음 납부일을 기존 주기 규칙대로 이동한다.
- [x] 리포트 추계소득 계산기와 재산세 계산기는 선택한 외화 입력을 최신 환율로 원화 변환한 뒤 기존 계산 결과를 갱신한다. **USD 100 추계소득 및 JPY 1,000,000 재산세 입력을 브라우저에서 검증함.**
- [x] 390px 모바일에서 통화·금액·환율 그룹이 한 열로 접히고 버튼·상태 문구가 화면 밖으로 넘치지 않는다. **거래 폼·고정지출 모달 모두 가로 넘침 0px 확인.**
- [x] Supabase schema 0.06 적용 후 generic 필드·CHECK·RLS·정책·schema meta를 운영 DB에서 검증했다. 기존 운영 행은 두 대상 테이블 모두 0건이어서 실제 legacy 행 무변경 대조는 해당 없음.
- [ ] 새 generic 필드가 실제 로그인 두 기기 간 LWW·canonical과 0.01/0.02/0.03 백업 복원에서 보존되는지는 실기기에서 왕복 확인한다.

## 13. 반복 납부일 자동 계산 (0.61)

- [x] 새 고정지출에서 `매월`을 고르면 1~30일·말일 선택만 보이고, 직접 날짜 입력은 보이지 않으며 다음 납부 예정일이 오늘 기준으로 즉시 계산된다. **앱 내 브라우저 검증됨.**
- [x] `매년`을 고르면 결제월·결제일 선택으로 바뀌고, 월별 실제 일수에 맞춰 일 선택지가 줄어든다. 2월 29일은 허용하되 평년에는 2월 말일로 계산한다. **2026-08-04 기준 매년 2월 29일 → 2027-02-28, 로직·브라우저 검증됨.**
- [x] 기존 고정지출 수정 시 저장된 `billing_anchor_day/month`가 선택값으로 승계되고, 반복 규칙을 바꾸지 않으면 기존 미납 `next_due_date`를 조용히 건너뛰지 않는다. **기존 매월 10일·2026-09-10 승계 확인.**
- [x] 반복 규칙을 매년 5월 19일로 바꾸어 저장하면 목록에 `매년 5월 19일`, 다음 `2027-05-19`가 함께 표시된다. **앱 내 브라우저 저장 흐름 검증됨.**
- [x] 매월 말일은 2월 말일로 보정한 뒤 3월 31일로 복귀하고, 매년 2월 29일은 윤년 원일자·평년 말일을 사용한다. **실제 도메인 로직 테스트 검증됨.**
- [x] 예정일보다 이르게 실제 거래를 입력해도 다음 회차로 한 번 전진하고, 밀린 거래는 거래일 뒤 첫 일정까지 전진한다. **실제 도메인 로직 테스트 검증됨.**
- [x] 일시중지·해지 항목을 다시 사용하면 오늘 이후 가장 가까운 반복 일정으로 예정일을 다시 계산한다. **서비스 경로 코드 추적 및 동일 순수 계산 테스트 검증됨.**
- [x] 데스크톱과 390×844 모바일에서 월간/연간 입력과 자동 예정일 카드가 가로로 넘치지 않는다. **문서·모달 overflow 0px, 콘솔 오류 0건.**
- [ ] 로그인한 실제 두 기기에서 반복 규칙 수정·재개·거래 연결 후 `billing_anchor_day/month`와 `next_due_date`가 LWW·canonical 동기화로 일치하는지 왕복 확인한다.

## 14. 고정지출 금액 정렬·환율 기준일 (0.62)

- [x] 데스크톱 고정지출 목록에서 USD 원금액과 KRW 금액의 오른쪽 끝선이 같은 열에 정렬되고, 보조 환산액만 아래 줄에 배치된다. **앱 내 브라우저 DOM 실측: 두 셀 right 723.2578px, 차이 0px.**
- [x] 외화를 선택하면 `환율 기준일`이 보이고 KRW로 바꾸면 숨는다. 기준일 변경 시 해당 날짜 이하의 최근 CBU 고시환율과 실제 고시일이 갱신된다. **USD→KRW 전환 시 기준일·환율 필드 모두 비표시 확인.**
- [x] 주말·공휴일 기준일은 선택일과 직전 실제 고시일을 함께 보여주며, 미래 기준일은 오늘까지 최신 환율을 사용한 예상치라는 안내가 보인다. **2026-08-02→2026-07-31, 미래 2026-08-20→2026-08-04 예상 문구 확인.**
- [x] 과거 기준일의 네트워크 fallback은 선택일보다 뒤에 고시된 캐시를 사용하지 않는다. **실제 ExchangeRateService 로직 테스트 검증됨.**
- [ ] 저장 후 목록·상단 월 환산액·모달 요약이 저장된 기준 환율로 일치하고, `거래 입력` 후 생성되는 실제 거래는 거래일 환율로 별도 확정된다. **목록·상단·모달 요약의 저장 기준 환율 일치는 USD 25 저장으로 확인, 실제 거래 생성은 별도 수동 확인.**
- [ ] 390px 모바일에서 환율 기준일·환율 입력·환산 안내와 금액 목록이 가로로 넘치지 않고 콘솔 오류가 없다.

## 15. 거래 입력 필드 정렬 (0.64)

- [x] 계정과목 `선택 가이드`가 계정과목 라벨 오른쪽 같은 줄에 보이고, 거래일·거래처·계정과목 입력 상자의 윗선과 아랫선이 정확히 일치한다. **앱 내 브라우저 실측: 세 필드 top 302.34375px·bottom 344.34375px·height 42px, 차이 0px.**
- [x] 계정과목 용어 버튼과 `선택 가이드`를 각각 눌러 기존 툴팁·가이드 이동 기능을 사용할 수 있는 DOM 구조와 접근성 이름이 유지되고, 콘솔 오류가 없다.

## 16. 모바일 고정지출 요약 (0.65)

- [x] 561~780px에서도 아이콘뿐 아니라 `고정지출현황`과 `월 환산액 · 활성 건수`가 보인다. **780px 실측에서 copy display=grid, 버튼 150×42px.**
- [x] 560px 이하에서는 상단바가 두 줄로 배치되고, 둘째 줄의 고정지출현황 제목·금액·건수가 잘리지 않는다. **390px에서 상단바 116px, 버튼 108.4375×42px, 두 문구 모두 표시.**
- [x] 360px 이하에서는 동기화 상태 글자만 축약되며 각 버튼의 제목·접근 가능한 이름은 유지된다. **360px에서 고정지출 버튼 137×42px.**
- [x] 360px·390px·560px·780px에서 `scrollWidth === clientWidth`이고 390px 고정지출 버튼을 눌러 모달이 열린다.
- [x] 모바일 메뉴가 열린 상태에서도 `topbar 36 > scrim 35`이며, 사이드바가 덮지 않는 빠른 입력 버튼 중심의 hit-test가 `quickAddButton`을 반환한다.
- [x] BlueStacks Pie64 Android Chrome에서 호스트의 로컬 수정본과 공개 배포본을 열었다. **1920×1080·240 DPI 가로 모드에서 공개 `Accounting Ledger v0.65` 정상 렌더를 확인. 세로 모바일 판정은 위 10절 항목대로 미완료 처리.**

## 17. 모바일 고정지출 잘림·동기화 성능 (0.66)

- [x] 412×915에서 외화 금액·원화 환산액·환율 기준일이 카드 안에서 모두 보이고 가로 스크롤이 생기지 않는다. **document/modal/body/row/amount overflow 모두 0px.**
- [x] 모바일 금액 영역은 설명 영역 다음 행에 배치되고 보조 환산문구는 줄바꿈할 수 있다. **금액 top 498px, 설명 bottom 489px.**
- [x] 모달 제목은 내부 스크롤 중 상단에 남도록 `position: sticky`가 적용된다.
- [x] 테이블 pull은 최대 4개 동시 실행, 중복 동기화는 single-flight, 대기 변경은 테이블·삭제상태별 batch로 계획된다. **로직 240 assertions로 순서와 상한 검증.**
- [x] 배포된 공개 v0.66을 BlueStacks Android Chrome에서 열어 버전을 확인한다. **Pie64 Android 9, 1920×1080·240 DPI 가로에서 `Accounting Ledger v0.66` 확인. 세로 모바일 레이아웃은 412×915 Chromium 실측으로 별도 검증.**

## 18. 앱 감사 접근성·저장·증빙 URL (0.67)

- [x] 412×915에서 개인 가계부의 대시보드·거래·장부·증빙·리포트·설정·개발자·데이터·가이드·재산세 화면을 모두 열고 document 가로 overflow 0, 콘솔 error 0을 확인했다.
- [x] 개인 가계부 거래 `감사 테스트 식사` 11,000원을 저장해 장부 표시와 새로고침 후 IndexedDB 영속을 확인했다.
- [x] 사업자 장부 거래 `감사 문구 구입` 11,000원을 저장해 소모품비 10,000원 + 부가세대급금 1,000원 = 보통예금 11,000원 균형 전표를 확인했다.
- [x] `termSearch`, `ledgerQuery`, `ledgerType`, `ledgerYear`, `industrySearch`에 각각 화면 의미와 일치하는 접근성 이름이 있고, 화면 전체의 표시 중인 이름 없는 입력·선택 컨트롤이 0개다.
- [x] 개인 가계부 설정 저장 안내가 사업자 정보라고 오표시하지 않고 `장부 정보를 이 기기에 저장했습니다.`로 나타난다.
- [x] 백업에서 복원될 수 있는 증빙 주소는 HTTPS만 클릭 가능한 링크·이미지로 사용하며, 그 외 scheme은 비활성 안전 안내로 표시한다. 자동 `app-audit` 23개 계약이 이 경로와 서명 APK·Android 백업·백업/XLSX 상한·tenant 보안 마이그레이션까지 고정한다.
- [x] 기준 커밋 `5c84d99` 보안 표준 감사 10건(High 4·Medium 4·Low 2)을 봉인했다. 9건은 작업 트리에서 보완했고, GitHub Pages 공유 origin은 전용 custom origin이 필요한 잔여 위험으로 분리했다.
- [ ] 운영 Supabase 로그인·RLS·다기기 canonical 왕복과 Cloudinary 실제 파일 업로드는 자격증명·복수 기기·외부 서비스가 필요한 별도 수동 검증으로 유지한다.

## 19. 원자 복원·cloud-first LWW·정본 RPC (0.68)

- [x] 두 store 백업에서 두 번째 store의 키 없는 행을 주입하면 사전검증에서 복원을 차단하고 첫 store의 `ORIGINAL-ATOMIC` 장부가 그대로 남는다. **실제 파일 chooser·IndexedDB 앱 경로 검증됨.**
- [ ] 정상 backup 0.04는 SHA-256을 검증하고 적용 전 store별 `이전→이후 (+추가/-제거)` 미리보기를 표시한다.
- [x] checksum·참조·전표 균형 중 하나를 바꾸면 IndexedDB 쓰기 전에 복원을 차단한다. **missing id는 브라우저, checksum 변조·불균형 전표는 실제 앱 순수 로직 테스트로 검증됨.**
- [x] “이 기기만 복원” 뒤 기존 pending queue는 `quarantined_by_restore`이며 자동 동기화가 복원 내용을 클라우드로 전송하지 않는다. **pending 표시 27건→복원 후 0건, 원본 장부명 유지, 성공 toast를 브라우저에서 확인.**
- [ ] 클라우드 백업은 `accounting_export_snapshot` 결과를 쓰고 증빙 원본 미포함 경계를 화면과 JSON `evidenceArchive`에서 확인할 수 있다.
- [x] 더 최신 cloud row와 오래된 local queue payload가 충돌하면 cloud 값이 유지되고 queue는 `superseded_by_remote`가 된다. **실제 `SyncAlgorithms.planQueueAgainstCloud` 회귀 테스트로 검증됨; 운영 로그인 왕복은 별도.**
- [ ] canonical expected version이 어긋난 동시 발행은 `CANONICAL_VERSION_CONFLICT`로 전체 rollback되며, 정상 발행은 version과 모든 표 변경이 한 번에 보인다.

## 20. 증빙 원본 암호화 독립 보관 (0.69)

- [x] 실제 Chromium에서 활성 증빙 원본을 Cloudinary delivery URL로 내려받아 `.bjea`로 다운로드하며 파일명에 v0.69가 포함된다. **Required `browser-roundtrip` 자동 검증.**
- [x] 외부 envelope에는 원본 파일명과 원본 base64가 평문으로 포함되지 않고 AES-256-GCM·PBKDF2-SHA-256 프로필만 공개된다.
- [x] 틀린 비밀번호와 한 글자 변조한 ciphertext는 Cloudinary upload 0회 상태로 차단되고 기존 `evidence_files.secure_url`이 유지된다.
- [x] 정상 아카이브는 Cloudinary에 원본을 정확히 1회 재업로드하고, 새 public_id/secure_url·SHA-256·감사로그·동기화 queue를 원자 반영한다.
- [x] CSP가 `res.cloudinary.com` delivery fetch를 허용하며 해당 connect-src 위반 콘솔 오류가 없다.
- [ ] 실제 운영 Cloudinary preset으로 이미지와 PDF 각각 백업·다른 브라우저 프로필 복원을 실행해 provider의 CORS·용량 정책을 확인한다. 자동 게이트는 외부 과금·자격증명 없이 같은 앱 경로를 mock한다.
